# concepts 更新履歴

## 2026-08-29
- distilled: restore-maui-picker-selected-command (ADR 起票なし — 既存 maui/ADR-0008・maui/ADR-0012・cross/ADR-0016 からの導出で足りる / maui/api/maui-facade.md に `PickerCell.SelectedCommand` の完了通知契約を追記 / cross/conventions/sample-parity.md の platform 固有画面の適用例に `MauiSpecificCellFeaturesDemoPage` を追加)

## 2026-08-25
- distilled: adjust-section-spacing (core/ADR-0027・android/ADR-0017 起票 / core/styling/list-appearance.md・core/styling/style-resolution.md 更新 — 既定 sectionMargin の style 間・platform 間統一、Section H/F text 間隔 4pt/dp と Root 0、Switch 状態色の accent 由来化)
## 2026-07-17

- Kasane 導入
- drift: クイック全体。構造異常なし、検証対象 concept 0 件 / ADR 0 件。
- migrated: `monorepo-foundation`, `settings-view-core` から5概念を確定。
- drift: Core境界、Cell抽象、公開識別子、旧SSoT案内の差異を検出。ADR-0012 / ADR-0013をproposedとして起票。

## 2026-07-18

- reviewed: ADR-0012案を却下し、Maven `groupId` はADR-0002どおり `jp.kamusoft` を維持。製品・成果物名は `artifactId` で表すよう `conventions/public-identifiers.md` を訂正。
- accepted: ADR-0013を確定し、ADR-0003をsupersededへ更新。
- drift: Android 3モジュールの現行Gradle `group` は `jp.kamusoft.kssettingsview` であり、採用済み方針 `jp.kamusoft` と不一致。コード修正は本移行の対象外として後続対応。
- migrated: `settings-view-ios-host`, `settings-view-ios-swiftui`, `settings-view-android-host`, `settings-view-android-compose` から5概念を追加し、表示状態同期の概念を更新。
- reviewed: 宣言ツリーIDの優先順位はADR-0008を維持し、現行コードとの差を未解消driftとして後続探索へ送付。iOS初回表示前Diff、無効化modifier、Android予約viewTypeは実装不具合候補として移行範囲外に記録。
- deferred: Android Root Header / Footerの再bind時復元契約は未確定のためconceptsへ含めず、後続探索へ送付。
- migrated: `settings-view-ios-style`, `settings-view-ios-theme-bridge`, `settings-view-android-style`, `settings-view-android-theme-bridge` から4概念を確定。
- reviewed: Androidのicon描画反映、fontFamily、iOSのseparator色とTheme再適用は実装不具合候補として移行範囲外へ記録。
- deferred: 未消費のStyle公開値とAndroidのplatform fallback差異はconceptsへ含めず、後続探索へ送付。
- migrated: `cell-types-basic` から2概念を確定。
- reviewed: URL画像、iOS trailing構成、Android共通行の旧レイアウト記述は、現行コードとaccepted ADRを正として統合。
- deferred: Android Buttonの共通行経路逸脱とRadioの再タップ通知差は実装不具合候補として移行範囲外へ記録。
- migrated: `samples-ios`, `samples-android` は独立概念を作らず、Sampleの責務境界とAndroidのMaterial3ホストtheme要件を既存2概念へ統合。
- reviewed: Sampleの画面構成、構築手順、旧Theme名、Radio内部widget、Gradle検証構文は現行コードとテストを正とし、旧specを歴史資料として凍結。
- deferred: Sample READMEとplatform guideの古いデモ一覧・利用者定義Cell参照は、移行完了時の周辺案内更新へ送付。
- remigrate 開始: 旧規約 (腐り度単軸 lint) 下で生成された concepts 全 18 ファイルを `changes/remigrate-concepts/reference/old-concepts/` へ退避し、改訂規約 (価値 lint・アンカー規約・用語規約・可読性規約) による再移行を起票。docs/ の内容は concepts へ吸収する方針を確定。rules.md・log.md は維持。

## 2026-07-19

- migrated: remigrate-concepts Batch A の `settings-view-core` / `cell-types-basic` から5概念を確定。実装済み入力 Cell 5種も code-first で回収。
- reviewed: 独立初見可読性レビュー PASS・オーナーレビュー承認。Core のADR候補4件は既存 ADR-0005 / 0009 / 0010 / 0013 に包含されるため新規起票なし。
- drift: 旧 spec / docs と現行 API の差7件は `changes/remigrate-concepts/candidates/batch-a-integration.md` に記録し、解消方向は未決定のまま後続へ送付。
- deferred: 表示状態同期の独立 architecture 概念は iOS / Android 両 platform の材料が揃う Batch D 横断統合へ送付。
- corrected: `cells/basic-cells.md` の基本 Cell 共通フィールドを現行 `ButtonCell` に合わせて訂正。全7種が `valueText` / `icon` / `hintText` を持ち、`ButtonCell` だけが `description` を持たない契約へ統一。
- migrated: remigrate-concepts Batch B の `settings-view-ios-host` / `settings-view-ios-swiftui` / `settings-view-ios-style` / `settings-view-ios-theme-bridge` から、iOS 固有の公開契約2概念を `platforms/` に確定。
- reviewed: 独立初見可読性レビュー PASS・オーナーレビュー承認。ADR候補は既存 ADR-0004 / 0006 / 0007 / 0008 / 0009 / 0010 / 0011 に包含されるため新規起票なし。
- drift: iOS の現行コード・旧 spec・docs・accepted ADR 間の差20件と Theme bridge の検証鮮度1件は `changes/remigrate-concepts/candidates/batch-b-integration.md` に記録し、解消方向は未決定のまま後続へ送付。
- deferred: Store・表示同期・Renderer Registry・宣言 Bridge・identity・style の横断概念は、Android 材料と統合する Batch D へ送付。
- migrated: remigrate-concepts Batch C の `settings-view-android-host` / `settings-view-android-compose` / `settings-view-android-style` / `settings-view-android-theme-bridge` から、Android 固有の公開契約2概念を `platforms/` に確定。
- reviewed: 独立初見可読性レビュー PASS・オーナーレビュー承認。ADR候補は既存 ADR-0004 / 0006 / 0007 / 0008 / 0009 / 0010 / 0011 / 0013 に包含。Material3 派生 XML Theme の要求は決定出典に採用理由・代替案がないため ADR を創作せず、platform 利用契約として保持。
- drift: Android の現行コード・旧 spec・docs・accepted ADR 間の差28件と Theme bridge の検証鮮度1件は `changes/remigrate-concepts/candidates/batch-c-integration.md` に記録し、解消方向は未決定のまま後続へ送付。
- deferred: Store・表示同期・Renderer Registry・宣言 Bridge・identity・style の横断概念は、Batch B の iOS 材料と合流する Batch D へ送付。
- migrated: remigrate-concepts Batch D の `monorepo-foundation` / `samples-ios` / `samples-android` と Batch B・C の繰越材料から、architecture 7概念・styling 4概念・conventions 1概念を確定。Sample は独立概念を作らず、consumer 境界と local source reference をリポジトリ境界へ統合。
- reviewed: 独立初見可読性レビュー PASS・オーナーレビュー承認。横断 ADR 候補は既存 ADR-0001 / 0002 / 0004 / 0006〜0011 / 0013 に包含されるため新規起票なし。
- drift: Maven 公開規範 `jp.kamusoft` と現行開発用 Gradle `group`、Core の platform 非依存説明、Sample / docs / frozen spec、宣言 identity の併用優先順位など横断所見12件を `changes/remigrate-concepts/candidates/batch-d-integration.md` に記録。
- deferred: toolchain baseline は build metadata へ委ねて独立概念化せず、Material3 派生 XML Theme の ADR 化は採用理由・代替案の出典がないため後続変更へ送付。Maven 公開 metadata と MAUI 識別子は実装時に再評価。
- migrated: remigrate-concepts Batch E で docs 8文書を code-first に残差走査し、Android composite build の SDK 解決境界、任意 View Accessory の公開入口、ButtonCell の alignment、style の特殊解決、Header / Footer と separator の外観契約を既存5概念へ統合。新規 concept・ADR は追加せず、最終構成を21概念とした。
- corrected: Batch A 文書に残っていた concepts root 基準の旧相対リンク24件を実配置基準へ修正し、`concepts/index.md` の「再移行中」注記を除去。iOS Theme 更新は表示済み Header / Footer の即時再適用を保証しない現行境界へ記述を統一。
- reviewed: 全21概念と index の独立初見可読性レビューは修正後 PASS。最終配置基準の相対リンク切れ0件、index 登録21件と本文21件の一致を確認し、オーナーレビュー承認を得た。
- drift: docs 残差走査の所見30件（重複を含む）と、前回までの未解消 drift・実装不具合候補・deferred・検証鮮度を `changes/archive/2026-07-31-remigrate-concepts/candidates/batch-e-integration.md` に集約。解消方向は本移行で決めず、後続変更へ引き継ぐ。
- deferred: docs 8文書の concepts 入口へのスタブ化と古い API / OpenSpec SSoT 案内の除去は、本移行と分離した後続変更として提案する。`docs/legacy-aiforms-reference.md` は歴史資料として対象外を維持。
- completed: `remigrate-concepts` の Batch A〜E を確定し、改訂価値 lint と可読性規約による concepts 再移行を完了。
- distilled: `remigrate-concepts` (ADR-0014 accepted / concepts は Batch A〜E の更新を確定 / migrate 級のため deviation 対象外)。

## 2026-07-31

- restructured: cross/ADR-0015 に基づき concepts / decisions をドメイン軸 (core / ios / android / maui + cross) で再配置。21概念を core 15・ios 2・android 2・cross 2 へ移動し、旧 `platforms/` カテゴリは各 platform ドメインの `api/` へ改称。ADR 14件は core 10・cross 4 へ番号温存のまま移動 (採番規則は decisions/index.md)。index は「薄い地図 + ドメイン別 index」の二層へ、rules.md にドメイン導出規則を追加。相対リンクは実配置基準へ修正し、リンク切れ0件を確認。config.yaml に domains / domain-skills を定義し、docs-refresh スキルの影響マップも新パスへ更新。
- concept (直接入力): `cross/conventions/sample-parity.md` を新規作成。Sample アプリを全 platform で同一文言・同一画面構成にするプラットフォーム間検証規約 (オーナー指示起点、samples/ios・samples/android のコード照合で現状差異を確認済み)。決定成分は cross/ADR-0016 として proposed 起票。`cross/architecture/repository-boundaries.md` に「製品契約にしない」と「platform 間で揃える」が別軸である旨の相互リンクを追随。初見可読性レビュー実施 (deviation.md の参照先明示・MAUI 未実装時の適用範囲の言い切り等を修正して PASS 相当)。現状の具体差異一覧は揮発性のため concepts へは移さず、解消は後続変更へ送付。
- revised (オーナーレビュー反映): `cross/conventions/sample-parity.md` と cross/ADR-0016 の「全 platform 同時変更必須」を緩和。一致は収束状態への要求とし、実装順序による一時的な片側先行は追随の追跡 (tasks / deviation.md / 後続 change) を条件に許容する形へ改訂。
- accepted: cross/ADR-0016 (Sample をプラットフォーム間パリティ検証装置と位置づける) をオーナー承認により確定。
- concept (直接入力): `cross/conventions/aiforms-origin-reference.md` を新規作成。移植元 AiForms.Maui.SettingsView / NativeCollectionView の在り処と参照ルール (移植完了までの時限規約、type: reference)。参照先の実在・リモート・upstream 更新状況をコード照合で検証済み。成り立ちの決定は cross/ADR-0017 として proposed 起票。移植元ソースのコピー配置は検討の上で不採用 (upstream 更新継続・GitHub から再取得可能なため)。docs/legacy-aiforms-reference.md は凍結歴史資料のまま温存し、新概念から注記付きポインタで接続。初見可読性レビュー PASS (index/log の参照リンク化・ksn-drift 語の補足・shim 禁止の理由明記を反映)。
- accepted: cross/ADR-0017 (AiForms.Maui.SettingsView を Native ベースへ移植・リファインする) をオーナー承認により確定。

## 2026-08-01

- distilled: `align-sample-parity` (新規 ADR なし / concepts 2件更新 / deviation 6件 / lessons inbox 2件は閾値未達で保留)。Sample のプラットフォーム間パリティ規約への適合実装 (iOS を正に Phase 1 → Android 同期 Phase 2、L 級)。design.md の全 Decision が cross/ADR-0016 の適用判断の範囲内で覆すコストも局所的なため、ADR 選別3基準を通らず新規起票なし (exploration.md / design.md の申し送りと一致)。
- updated: `cross/conventions/sample-parity.md` の「画面構成の一致」に色パラメータ (`accentColor` 等) を明記し、platform 固有の semantic color を使わず同一 RGBA を Sample 共通定義に置く規定を追加。ホスト側レビューが「表示文言」軸の機械照合で色差を構造的に拾えず、相方 (codex) の独立レビューだけが検出した穴を規約化したもの。あわせて「許容される差異」に、Sample が明示的に渡していないパラメータの本体既定値 platform 差 (`titleColor` 未指定の ButtonCell が iOS 青 / Android 紫) を追加。
- updated: `android/api/android-native-host.md` に「ホスト Activity の前提」を新設。Material 形式の TimePicker / DatePicker が DialogFragment 経由のため host Activity に `FragmentActivity` を要求し、`ComponentActivity` では例外もログもなく無反応になる公開契約を記録。これまで concepts・docs とも記述0件で、本変更で Android Sample を `ComponentActivity` → `FragmentActivity` 化して初めて picker が機能した実害が出典。
- concept (直接入力): `cross/conventions/comment-policy.md` を新規作成。全言語共通 (Swift / Kotlin / C# ほか) のソースコメント規約 — 許容参照3種 (`<domain>/ADR-NNNN` / URL / 恒常規格) と禁止参照・禁止記述類型・書き換え3類型 (imported: KsCoreApi `kasane/concepts/conventions/comment-policy.md` を本プロジェクトへ適応)。C# 限定を言語非依存へ一般化し、禁止参照はコード照合 (grep) で実在確認した形 (openspec パス・kasane 変更名裸参照・Phase / Decision / Major 通番) へ差し替え。AiForms 互換仕様の現在形記述は `aiforms-origin-reference.md` の時限規約と整合させ許容。ADR 成分なし (規約単体で自己完結)。初見可読性レビュー実施 (重要4件: ADR 選別基準の裸参照解消・「仕様書への参照」の矛盾解消・リポジトリ内コード識別子参照の許容明記・適用契機と検出手段の追記、ほか軽微6件を反映して確定)。既存コードの違反 (禁止参照 約330件・履歴記述 約86件、主に ios/) は本規約導入と分離し、後続の変更フローへ送付。
- deferred: 本体側の後続課題を `changes/archive/2026-08-01-align-sample-parity/` の deviation.md / review-001.md / review-002.md へ記録して送付 — (a) FragmentActivity 要求を満たさない場合に無言で失敗する挙動の改修、(b) `DatePickerUIStyle.Spinner` が spinner モードにならない実装バグ (`calendarViewShown` はモード切替手段ではない)、(c) PickerCell の上限超過時にチェックボックス表示が戻らない不具合、(d) Android の NumberPickerCell `unit` / DatePickerCell `todayText` の公開 API パリティ、(e) 本体 iOS の既定青の2系統分裂 (`defaultAccentColor` #007AFF ≠ `defaultButtonTitleColor` = `.systemBlue` #0088FF)。入力 Cell 5 種デモの NumberPickerCell footer 中立文言化は spec / mock 改訂を伴う論点として後続へ。
- concept (直接入力): `cross/conventions/test-execution.md` を新規作成。iOS の 3 テストターゲットはいずれも `#if canImport(UIKit)` ガードを含み、macOS 上の `swift test` では UIKit 依存テストがコンパイル対象から外れる (`KsSettingsViewUITests` は全ファイルがガードのため 1 件も実行されない) ため、検証は `cd ios && xcodebuild test -scheme KsSettingsView-Package -destination 'platform=iOS Simulator,name=<機種名>'` で行い実行件数の確認までを検証とする規約。オーナー指示起点 (「UI テストを走らせるのは必須なので concepts の規約に配置する」)。ガード分布 (Core 2/12・SwiftUI 10/11・UI 19/19)・scheme 一覧・`swift test` 88 件 / `xcodebuild test` 338 件・`-only-testing` による絞り込み (Core 83 件)・リポジトリルート実行の失敗をすべて実測で照合済み。ADR 成分なし (規約単体で自己完結)。初見可読性レビュー実施 (重要2件: `KsSettingsViewSwiftUITests` も 10/11 ガードで同じ事故が SwiftUI 層で再現しうる点の明記・`<機種名>` の解決手段追加。中3件: README / docs の `swift test` 案内との関係を明記・「ホスト」が `ios/api/ios-native-host.md` の UIKit Host と衝突する多義語のため「macOS 上の `swift test` 実行」へ統一・件数記述を「差の大きさが本質」の形へ。軽微4件: tags 追加・description の実態合わせ・報告形式の具体化・関連節の追加。すべて反映して確定)。lessons/inbox の同型パターンは本 concept へ昇格につき削除。
- deferred: `README.md` と `docs/overview.md` の iOS 手順が `swift test` を「ユニットテスト」として案内しており、`cross/conventions/test-execution.md` の完了判定と読み手が混同しうる。concept 側に「利用者向けの動作確認であり完了判定とは別物」と明記して吸収したが、README / docs 側の追随は docs-refresh スキル経由の後続作業として送付する。
- distilled: `fix-cell-accessory-vertical-fill` (新規 ADR なし — ios/ADR-0001 は探索時に起票・実装前に accepted 済み、コード参照コメントも配置済み / cell-row-layout.md 更新 / deviation 1件 / lessons inbox 捕捉1件・昇格なし)。iOS の Cell 級アクセサリを contentStack 外のアクセサリ列へ移し、description の回り込みを解消する M 級変更。deviation の「valueText とアクセサリの間隔 約 6pt 維持」は可逆・局所的な視覚パラメータ判断のため ADR 選別3基準を通らず起票なし (記録は deviation.md)。
- updated: `core/styling/cell-row-layout.md` の視覚文法を trailing 2 系統 (Cell 級アクセサリ = セル全体に対し垂直センター / 行内 trailing = title と同じ主行) へ改訂し、「description はアクセサリの領域と重ならない幅で折り返す」を保証として明文化。platform 表の iOS 共通行を旧記述 (UIKit configuration) から `KsListCellBase` の自前 `UIStackView` 階層へ追随。Android EntryCell の accessory 領域配置は platform 差として注記。
- concept (lessons 昇格): `cross/conventions/runtime-behavior-verification.md` を新規作成。実行時挙動 (IME・フォーカス・アニメーション・タイミング・OS 連携) が絡む不具合修正の完了判定を「①修正前に実環境で症状を再現 ②修正後に同一手順で解消確認 (可能なら A/B) ③証跡を change 配下に残す」と定め、原因分析でもコード読解のみの真因断定を禁止する規約。出典は fix-entrycell-ime-composition (コード読解のみの原因断定で修正し、ユニットテスト全 green・レビュー APPROVED まで通したがオーナー実機確認で未解消と判明した事例)。lessons/inbox の runtime-fix-reported-without-device-verification (count 1) をオーナー提案 (「conventions 入りしても良さそう」) により閾値前倒しで昇格・削除。強制力ルーティング上は「機械化できない作業規律 → lessons/impl.md」だが、impl 単独 scope に閉じない検証規律であることと test-execution.md の前例により conventions 配置とした。
- distilled: `fix-entrycell-ime-composition` (android/ADR-0001 を proposed 起票 — 内容更新は payload 付き通知 + change アニメーション無効で同一 ViewHolder を維持、bind 内 IME 破壊系 setter は差分ガード。decisions/android/ を新設 / android/api/android-native-host.md 更新 — 保証に「内容更新は同一 ViewHolder への再 bind で composing を破壊しない」を明文化し、独自 CellViewHolder 作者向けに inputType 等の差分ガード指針を追記 / deviation なし (S級・デルタスペックなし) / lessons: 実装 scope の教訓はオーナー承認で cross/conventions/runtime-behavior-verification.md へ昇格済み (前エントリ)、code-review scope の見逃し (実機証跡なしで実行時修正を APPROVED) を inbox へ新規捕捉 count 1)。Android EntryCell の日本語 IME 即時確定を解消した S 級変更。当初の差分ガード単独修正が実機で無効と判明し、codex 並走調査で真因 (payload なし notifyItemChanged + DefaultItemAnimator の ViewHolder 差し替え) を再特定した経緯を含む。
- distilled: `fix-android-cell-width-allocation` (新規 ADR なし — android/ADR-0002 は探索時に起票・実装前に accepted 済み、コード参照コメント 9 箇所配置済み / concepts 2件更新 / deviation 4件は全件オーナー承認済み・補完なし / lessons inbox 捕捉4件、昇格閾値到達なし)。Android 共通行を水平 LinearLayout + weight 化し EntryCell の入力欄へ残り幅を配分した M 級変更。deviation 4件 (title 単一行化 / ButtonCell titleAlignment 実効化 / 6dp クリアランス / 原典スクリーンショット未取得) はいずれも ADR-0002 の帰結で可逆・局所的なため ADR 選別3基準を通らず、記録は deviation.md と concepts への反映で完結。
- updated: `core/styling/cell-row-layout.md` — 「Android の EntryCell は入力フィールドを accessory 領域に置く既存配置を維持する」を撤回し行内 trailing (両 platform) へ訂正。「主行の幅配分」節を新設 (既定 = title 残り幅 / EntryCell = 入力フィールド残り幅・幅 0 許容 / trailing なし = title 全幅、title 単一行 + 末尾省略と description 複数行の非対称、6dp クリアランスが content box に効くため ButtonCell 全幅中央揃えに 3dp オフセットが生じる原典同型の帰結)。初見可読性レビューの指摘で「trailing control」の多義を解消・「主行」を定義・AiForms と ADR への参照をリンク化・h1 追加。
- updated: `cross/conventions/test-execution.md` — 予告されていた Android 節を実測に基づき追記 (`cd android && ./gradlew test`、debug/release 両 variant 1070 件、up-to-date スキップ対策の `--rerun-tasks`、件数の確認先 test-results XML)。「Robolectric で検証したつもりになる描画系アサーション」を新設: 実 ellipsize は `@GraphicsMode(NATIVE)` 必須 (legacy では `getEllipsisCount` 常に 0)、singleLine TextView の実描画位置は `dispatchOnPreDraw()` + `getLineLeft(0) - scrollX` (content box 起点) で測る。件数併記の規約を platform 横断の総則へ移動。出典: fix-android-cell-width-allocation の review-002/003 実測。
- distilled: `fix-entrycell-enter-focus-crash` (android/ADR-0003 accepted — 単一行 EntryCell の Enter は IME_ACTION_DONE + View.OnKeyListener で「完了」として消費、コード参照コメント配置済み / core/cells/input-cells.md 更新 — プラットフォーム差に Android 単一行 EntryCell の Enter=完了 (キーボード閉鎖・フォーカス送りなし)・複数行 Enter=改行を公開契約として明文化 / deviation 1件 (生 KEYCODE_ENTER 消費へのスコープ拡張、オーナー承認) / lessons inbox 捕捉1件・昇格なし)。リスト最後の EntryCell で Enter 押下時の `focus search returned a view that wasn't able to take focus!` クラッシュを解消した S 級変更。imeOptions のみの初回案が実機で生 Enter に無効と判明 (IME が DOWN を消費し UP 単独配達になるため OnEditorActionListener の enterDown ゲートが成立しない) し、View.OnKeyListener 方式へ転換した経緯と実機 A/B 証跡 (evidence/) を含む。

## 2026-08-02

- distilled: `fix-android-chevron-vertical-centering` (android/ADR-0004 accepted — Cell 行のテキストとアクセサリは幾何中央ではなく光学中央で揃える。コード参照コメントは CellBaseLayout.kt に配置、画像リソースはオーナー指示で対象外 / core/styling/cell-row-layout.md 更新 — 視覚文法に光学中央補正の許容を追記 / deviation なし (S級・デルタスペックなし。原典 AiForms からの逸脱は ADR に記録) / lessons inbox 捕捉2件 — 画素測定裏取り success・ラッパより原データ修正 pain、いずれも count 1 で昇格なし)。Android の chevron drawable パス非対称 (原典由来・約 1dp 上浮き) とフォントメトリクス由来のテキスト沈み (約 1dp) の複合による垂直ズレを解消した S 級変更。includeFontPadding 仮説が実測で棄却された経緯と Pixel 6a の画素測定証跡 (ui/references/) を含む。
- distilled: `android-picker-selection-sheet` + `ios-picker-selection-parity` (新規 ADR なし — android/ADR-0005 は探索時起票・追補2件込みで accepted 済み、コード参照コメント配置済み。候補4件 — 初期スクロール / 確定ラベル色 = Theme.backgroundColor / 全展開のドラッグ限定 / KsSimpleCheckView 再利用 — は可逆・局所的で選別3基準を通らず concepts 側へ / concepts: 新概念1件 + input-cells.md 追随 / deviation: android 4件は記録済み・補完なし、ios は既知制約1件を新規記録 / lessons: 既存パターン count+1 が3件、新規捕捉5件、昇格閾値到達なし)。PickerCell の選択 UI を Android はボトムシートへ刷新し、iOS は選択面のスタイル継承・初期スクロール・a11y で追随させた M 級ペア変更。
- distilled: `timepickercell-color-adjust` (新規 ADR なし — android/ADR-0006 は探索時起票・実装前 accepted 済みで、実機スパイクの結果による機構補正 — Decision 3 静的/動的分離・Decision 5 キャレット = `TextInputLayout.cursorColor`・Decision 6 入力欄枠の駆動 = EditText フォーカス — と追随確認リスト・現行照合 footer を反映 (review-002 Minor の申し送りどおり)。ダイアログ背景 = `Theme.backgroundColor` の選定・透過率定数 5.5%/16% は可逆・局所的で選別3基準を通らず ADR 化見送り / concepts: `core/cells/input-cells.md` 更新 — Android TimePickerCell ダイアログのテーマ配色契約 (色ロール・アクセント3段解決・黒白自動選択) をプラットフォーム差へ追記 / deviation なし — spec との乖離なし。回転時の配色喪失は既存構造由来として `fix-picker-dialog-recreation` へ切り出し済み / lessons: comment-policy 違反 count 2→3 で昇格候補到達 (ルーティング第一候補は lint/hook、オーナー承認待ち)、スパイク先行タスクの成功を新規捕捉 count 1)。Android TimePickerCell の時刻選択ダイアログにテーマ配色を動的適用した M 級変更。
- concept (distill): `core/cells/picker-selection-surface.md` を新規作成。PickerCell 選択面のクロスプラットフォーム共通契約 (タイトル解決・確定/破棄の意味論・上限+触覚フィードバック・accent 3段解決・スタイル継承・初期スクロール・a11y・モデル値の非正規化) と意図的なプラットフォーム差 (OS リソースの操作ラベル・Android ヘッダー文字契約・iOS ナビバーのシステム既定サイズ維持・全展開ジェスチャー) を、両 change のデルタスペック・deviation・ADR-0005 追補から統合。初見可読性レビュー実施 (重要: 作業状態の未定義・スワイプ dismiss の矛盾・非正規化の帰結不明・cellTitleFontSize 参照の宙浮き等を修正して確定)。input-cells.md からリンク接続。
- distilled: `android-numberpicker-modern-ui` (新規 ADR なし — android/ADR-0007 は探索時起票・実装前 accepted 済みでコード参照コメント 8 箇所配置済み。レビュー起因の設計修正 — スナップ静止判定の残距離 0 条件・候補列の index ベース遅延生成 — は可逆・局所的で選別3基準を通らず ADR 化なし / concepts: 新概念 `core/cells/number-picker-selection-surface.md` 作成 + `core/cells/input-cells.md` 追随 — 「NumberPickerCell.unit は iOS 固有」のプラットフォーム差記載を解消し、valueText 自動表示の unit 適用を iOS / Android 共通契約へ / deviation なし — verify-001・review-002 とも未記録乖離 0 件、オーナー確認済み / lessons: test-asserts-proxy-not-real-path count 1→2、新規捕捉2件 — idle-callback-assumed-snap-settled (impl)・spec-literal-compliance-excuses-runtime-infeasibility (code-review)、昇格閾値到達なし)。Android NumberPickerCell の unit パリティ追加と選択 UI のボトムシート + 自作ホイール刷新 (M 級)。review-002 申し送りの記録: `changes/archive/2026-08-01-align-sample-parity/deviation.md` の「Android は unit 相当の公開 API を持たない」および deferred (d) の unit パリティ欠落は本 change で解消済み (archive は凍結のためここに記録)。
- concept (distill): `core/cells/number-picker-selection-surface.md` を新規作成。NumberPickerCell 選択面のクロスプラットフォーム共通契約 (pickerTitle 解決・step 刻み候補生成・候補表示への unit 適用と valueText 非適用・初期選択 = value・確定のみ 1 回発火 / 非確定 dismiss 無発火) と意図的なプラットフォーム差 (Android = ボトムシート + スナップ式ホイール android/ADR-0007・OS リソースの操作ラベル・スナップ静止の意味論・a11y、iOS = 埋め込み UIPickerView + ツールバー)、契約外の防御挙動 (min > max・Int 上限) を、デルタスペック・ADR-0007・両実装コードから統合。初見可読性レビュー実施 (PASS。中指摘 — valueText 未定義のまま規範の主語・「文字契約」造語・防御挙動が「意図的な差」節に同居・スナップ静止と保証の視覚/論理のずれ等 — を反映して確定)。input-cells.md からリンク接続。

## 2026-08-03

- distilled: `android-datepicker-spinner-wheel` (新規 ADR なし — android/ADR-0009 は探索時起票・実装前 accepted 済みで、コード参照コメントは DateSelectionSheet.kt / KsWheelView.kt / DatePickerCellViewHolder.kt 等に配置済み。実装中の年候補上限引き直し (Int 表現上限 → 提示上限 1,000,000 件、オーナー決定) は可逆・局所的で選別3基準を通らず ADR 化なし / concepts: 新概念 `core/cells/date-picker-selection-surface.md` 作成 + `core/cells/input-cells.md` 追随 — 「DatePickerCell.todayText は iOS 固有」のプラットフォーム差記載を解消し、両 OS 対応 (Android は Spinner 限定・Material 無視) へ更新 / deviation なし — verify-002 は全 31 Scenario 一致 (VALID)、オーナー確認済み / lessons: spec-scenario-premise-unreachable-in-input-domain は実装フェーズで即時捕捉済み (count 1)、追加の取りこぼしなし)。Android DatePickerCell の Spinner 選択 UI をボトムシート + 3連 KsWheelView へ刷新し todayText パリティを追加した M 級変更。
- distilled: `datepickercell-color-adjust` (新規 ADR なし — android/ADR-0008 は探索時起票・実装前 accepted 済みで、コード参照コメントは DatePickerColorizer.kt 等に配置済み / concepts: `core/cells/date-picker-selection-surface.md` に Material ダイアログの色ロール4種・accentColor 接続・ヘッダ重なり補正・表示セッション内限定・既知の限界 (カスタム calendar style の初回誤分類、deviation 受容) を集約 / deviation 2件 — カスタム calendar style の初回誤分類の受容 (オーナー決定 2026-08-02)・A案ヘッダの標準ベースライン復元で mock より間隔が広い (オーナー承認)。追加の未記録乖離なし (verify-001 全 Scenario 一致) / lessons: chat-attachment-extractable-from-transcript は探索時に即時捕捉済み。distill 補完2件 — spike-first-tasks-for-desk-determined-assumptions count 1→2 (実機スパイクが CJK 仮説を棄却し真因を実装前確定)・known-limitation-accepted-without-deviation-record を新規捕捉 count 1 (code-review。受容には deviation 記録が要る、の手続き論をオーナーが採用)。mock 間隔乖離の mock-shows-param-not-matching-current-impl への match はオーナー判断で見送り (「決定に沿った標準配置の復元」であり mock 値の写経ミスとは型が違う)。昇格閾値到達なし)。Android DatePickerCell (Material) のダイアログへテーマ配色を動的適用しヘッダ重なりを補正した M 級変更。
- concept (distill): `core/cells/date-picker-selection-surface.md` を新規作成。DatePickerCell 選択面のクロスプラットフォーム共通契約 (pickerTitle 解決・確定のみ1回発火 / 非確定 dismiss 無発火・min/max 範囲保証・todayText の提示条件と範囲外セーフガード・iOS の時刻成分保持) と、uiStyle × プラットフォームの器 4 形式、Android Material ダイアログの配色契約 (ADR-0008)、Android Spinner の3連ホイール契約 (ADR-0009 — 年候補既定 1900-2100・日候補追随と末日丸め・防御上限・Locale 表示導出) を両 change のデルタスペック・deviation・実装コードから統合。初見可読性レビュー実施 (重要2件: Material の uiStyle case 名と Material Components ライブラリの二義解消・「通常セル」を「日付マス」へ。中5件: androidButtonColor の所有者明記・「系列」の定義・スナップ静止の一言定義・実効面の書き下し・64bit 算出の意図明記等。すべて反映して確定)。input-cells.md からリンク接続。

## 2026-08-03 (2)

- distilled: `datepickercell-today-shortcut` (新規 ADR なし — android/ADR-0010 は探索時起票・実装前 accepted 済みで、コード参照コメントは DatePickerTodayShortcut.kt / DatePickerColorizer.kt に配置済み / concepts: `core/cells/date-picker-selection-surface.md` 追随 — todayText の適用面を全形式へ更新し Material 固有契約 (表示月移動・代替表示状態・冪等性・ADR-0010 の駆動方式) を追加、「Material で効くと案内しない」の禁止事項を撤去、ライブラリ更新時の追随確認対象に今日ジャンプを追加。`core/cells/input-cells.md` の todayText プラットフォーム差記載も追随 / deviation なし — verify-001 は全 15 Scenario + Requirement 追加条項一致 (VALID) / lessons: known-deferred-issues-not-passed-to-reviewers を新規捕捉 (count 2)、spike-first-tasks-for-desk-determined-assumptions に成功3例目を追記 (count 3)。いずれも閾値未達で昇格なし / 申し送り: Fragment 復元後の今日ボタン消失と tag 世代サフィックスの相互作用を fix-picker-dialog-recreation の exploration.md へ記録済み)。DatePickerCell カレンダーモード (Material) へ todayText の「今日」ショートカットを追加した M 級変更。

## 2026-08-03 (3)

- distilled: `fix-picker-dialog-recreation` (ADR-0011 へ追補 — review-001 Major への対処で claim に「表示側が show 直後に claim し生きたダイアログを走査対象から除外する」第2の役割が加わったことを、review-002 の蒸留申し送りに基づきオーナー承認の追補として記録。新規 ADR 起票なし / concepts: `android/api/android-native-host.md` 更新 / deviation なし — verify-002 全 Requirement・Scenario 一致 (VALID)・review-002 APPROVED、未記録乖離 0 件をオーナー確認済み / lessons: 実装フロー中の即時捕捉3件を確認 — adr-rejected-failure-mode-not-forbidden-globally 新規 count 1 (spec-review)・mutation-probe-proves-assertion-power count 3 (成功3例目として追記済み)・known-deferred-issues-not-passed-to-reviewers count 2 (先行 change 由来)。distill 補完の取りこぼしなし (レビュー確定指摘は全件修正済みで的外れ却下なし、オーケストレーター裁定の降格2件はスコープ判断)、昇格閾値到達なし)。MaterialTimePicker / MaterialDatePicker の Activity 再生成復元をコンテナ駆動で実装した M 級変更。
- updated: `android/api/android-native-host.md` に「ピッカーダイアログの回転復元」節を新設し、保証することへ復元保証を1行追加。回転復元の利用者向け契約 — 完全復元の2条件 (再生成前後で同じ `Cell.id`・単独インスタンス構成)・dismiss フォールバックが常に安全側 (別 Cell への値書き込みは成否によらず起きない)・選択途中状態は Material の saved state 委ね・ボトムシート系は対象外 — を記録。proposal の「回転復元には安定 id を推奨」の docs 方針申し送りの知識面をここへ接地 (docs 側の追随は docs-refresh の責務)。

## 2026-08-03 (4)

- distilled: `fix-adapter-not-restored-on-reattach` (新規 ADR なし — 候補「resyncFromStore は root のみ取り込み Theme は StateFlow 再配信に任せる」は単一ファイル内・可逆・KDoc とガードテスト (M2) に焼き付き済みで選別3基準を通らず見送り / concepts: `android/api/android-native-host.md` 更新 — detach / 再 attach 契約を追随 / deviation なし — S 級・デルタスペックなし、review-002 が未記録乖離 0 件を確認。review-002 Minor の既存不具合 (初期 Theme の ItemDecoration 未追従) は `fix-decoration-theme-not-applied-on-initial-bind` として起票済みで無記録持ち越しなし / lessons: mutation-probe-proves-assertion-power に4例目を追記 (count 3→4)・review-recommended-fix-treated-as-hypothesis を新規捕捉 (impl / success / count 1 — review-001 の推奨修正形が誤りで、実装者の検証と代替設計が review-002 の M2 で正しさを実証された)。昇格閾値到達なし)。detach で切った adapter を再 attach で戻し、購読再確立前に Store 現在値を取り込み直して stale 復帰も防いだ S 級変更。
- updated: `android/api/android-native-host.md` — detach / 再 attach のライフサイクル契約を追随。「detach 時は購読停止 + Adapter 参照切断 (リーク防止)、再 attach 時は Adapter 復帰 + Store 現在状態の取り込み直し → 購読再確立」を明文化し、Store 更新通知が replay を持たないため detach 中の Diff は消えるが復帰は `store.state` / `store.theme` の現在値から行われること、ViewPager2 オフスクリーン / Compose `AndroidView` 付け外しでも最新内容で復帰すること (スクロール位置は対象外) を保証することへ追加。出典: fix-adapter-not-restored-on-reattach の実装 KDoc・review-002。

## 2026-08-04

- distilled: `add-cell-types-custom` (ADR 2 件起票 — core/ADR-0016「iOS 型消去内蔵 / Android ジェネリック」(design Decision 1 由来、実装後帰結の contentType 等価性参加を Consequences に追記)・core/ADR-0017「無効化は操作抑止優先、TalkBack 読み上げ非対称の受け入れ」(deviation のアクセシビリティ判断由来) / concepts: `core/cells/custom-cell.md` を新規作成 — content 値 + builder の公開契約・等価性と再バインド・挙動プロパティ・高さ/style・DSL 配置・カスタムセル3層 (①インライン ②ラップ関数 ③UserDefinedCell) の使い分け指針 (ADR-0014 Consequences の申し送りに対応)。初見可読性レビュー実施 (再バインドの定義欠落・strictMode / visible projection / KsAnyView の未説明語・content なし形の前提抜け等 11 件、すべて反映)。`core/styling/cell-visual-states.md` 更新 — CustomCell の無効表現例外 (content 全体 alpha 0.38・iOS 二重淡色化・TalkBack 非対称) を追記。`core/styling/cell-row-layout.md` 更新 — 適用範囲を「共通行構造を持つ Cell」と明記し CustomCell を適用除外として接続 (ADR-0015 の concepts 反映) / deviation あり — 発生時記録 6 項目 (無効時淡色化のオーナー指示・iOS Renderer 基底・Android 高さ解決・アクセシビリティ・縦位置・実機検証範囲)、verify-001 が全項目の記録済みを確認、未記録乖離 0 件をオーナー確認済み / lessons: 実装フロー中の即時捕捉 3 件 (orchestrator-blanket-ban / untracked-new-files / static-screenshots) を確認、レビュー顛末に的外れ却下なしで補完 0 件、昇格閾値到達なし (最接近: mutation-probe 4/5・spike-first 3/5))。iOS / Android 両プラットフォームへ CustomCell (任意宣言 UI の行) を新設した L 級変更。

## 2026-08-04 (2)

- distilled: `fix-decoration-theme-not-applied-on-initial-bind` (新規 ADR なし — A案 (`setRootDirect` 末尾での `applyDecoration` 呼び出し) は単一メソッド内・可逆・KDoc と退行テストに焼き付き済みで選別3基準を通らず / concepts: 変更なし — Android の separator 契約 (`list-appearance.md`) は既述どおりで、本修正はその契約への追随 / deviation なし / lessons: mutation-probe-proves-assertion-power に5例目を追記、visible-appearance-fix-reported-done-without-visual-check を新規捕捉 (process / pain / count 1 — 完了報告に視覚確認が無くオーナー指摘で実機 A/B を追加実施。副産物として iOS 側の同症状発見につながった))。初期 Theme 付き Store の bind で ItemDecoration だけが既定 Theme に取り残される S 級修正。Pixel 6a 実機 A/B でセパレータ色の反映を実測確認済み。
- distilled: `fix-ios-separator-color-not-applied` (新規 ADR なし — 「Theme のセパレータ色を separator へ適用する」は既定路線への復帰で決定に当たらず / concepts: `core/styling/list-appearance.md` 更新 / deviation なし / lessons: mutation-probe-proves-assertion-power に6例目・pixel-measurement-before-ui-alignment-fix に2例目 (配色系への拡張) を追記)。iOS 主リストに `Theme.separatorColor` の消費コードが存在せず常に UIKit 既定グレーだった S 級修正。iPhone 17 (iOS 26.5) シミュレータでピクセル実測確認済み。
- updated: `core/styling/list-appearance.md` — 「iOS の現行描画は `Theme.separatorColor` を separator へ適用しない」の既知制限記述を削除し、両 platform が main list の separator 色を `Theme.separatorColor` から解決する契約に更新 (モーダルのピッカー選択画面も同色)。separator は Theme の固定色で system appearance (light / dark) に追従しないことを併記 (review-001 申し送り)。保証することへ「初期 Theme・実行時 Theme 変更の両方で追従」を追加。timestamp 2026-08-04 に更新。

## 2026-08-05

- distilled: `add-maui-native-bridge` (ADR 1 件起票 — maui/ADR-0006「Android Binding は AndroidGradleProject ではなく gradlew 直接実行方式」(deviation.md + ksn-dual-research の実測記録由来、オーナー承認で accepted。Binding.Android.csproj に参照コメント追加)。落とした候補: Decision 8 (accessory text 限定 — phase-1 のスコープ限定で恒久契約でない)・replace 系の String? 戻り値化 (局所・additive → concepts へ記載) / concepts: `maui/api/native-bridge.md` を新規作成 (maui ドメイン初の概念。初見可読性レビュー実施 — Host/Store の未定義初出・DTO ID の出自不明・nullable 条件・「第三の経路」「縦疎通」等 20 件、すべて反映)、`core/architecture/store-and-update-streams.md` 更新 — iOS `replaceCells` 対称化で内容更新バッチ行を両 OS 対称に、バッチ契約 (入力順・空リスト no-op・未知 ID スキップ・適用0件無配信) を追記、`ios/api/ios-native-host.md` 更新 — Store 操作表へ `replaceCells(_:)` 追加とバッチ反映の1段落 / deviation あり — Android Binding の gradlew Exec 方式 1 件 (実測根拠つき・オーナー承認済み → ADR-0006)。verify-001 が未記録乖離 0 件を確認、オーナーにも追加乖離なしを確認済み / lessons: 即時捕捉2件を確認 (trash-rule-scope / review-recommended-fix-treated-as-hypothesis 2例目)、distill 補完3件 — deviation-cause-written-without-reading-source に2例目 (「SDK 制約」主張の反証)、proposal-named-artifact-format-not-checked を新規捕捉 (code-review / pain / count 1)、claimed-constraint-verified-by-dual-research を新規捕捉 (process / success / count 1)。昇格閾値到達なし (最接近: spike-first 3/5) / roadmap: maui-support phase-1 を completed 化、派生起票3件の論点を phase-2 / phase-4 agenda へ転記)。C# → Bridge → 内部所有 Store → Native Host の縦経路を LabelCell 1種で疎通させた L 級変更 (maui-support phase-1)。
- updated: `maui/index.md` を新設 (maui ドメインの最初の書き込み)、ルート `index.md` の maui 行をリンク化。

## 2026-08-05 (2)

- distilled: `fix-android-accessory-header-refresh` (新規 ADR なし — android/ADR-0012 は探索時起票・実装前 accepted 済みで、second-opinion-001 (codex spec-review) の Critical / Major 指摘による本文補正と deviation 解釈の注記までオーナー承認済み。コード参照コメントは KsSettingsListAdapter.kt / KsSettingsView.kt に配置済み / concepts: `core/architecture/display-state-synchronization.md` 更新 — proposal 申し送りの Android 欄追随 (下記 updated 行) / deviation 1 件記録済み (「旧∩新 visible の cell id」を通知対象の上限と解釈し値変化のみ通知 — ADR-0012 注記と対応)。verify-001 (VALID・全 16 Scenario 一致) で未記録乖離 0 件、オーナーにも追加乖離なしを確認済み / lessons: test-asserts-proxy-not-real-path に3例目 (positive テスト 2 件が修正前コードでも green) を追記し pain 3/3 到達 → オーナー承認により `lessons/test.md` L-001 へ昇格 (scope 新設。経緯は details/ へ移設)、async-wait-helper-times-out-silently を新規捕捉 (test / pain / count 1 — ホスト・相方が独立に同一指摘) / エミュレータ実機確認 4 項目 (verify-device.md — updateAccessory / replaceSection / 型切替の目視、review-001 Suggestion 6 消化済み))。Android の Section H/F と full 更新の内容取りこぼしを DiffCallback 内容比較と setRootDirect の一括 rebind で解消した M 級変更。
- distilled: `fix-replace-section-header-refresh` (新規 ADR なし — 局所バグ修正で選別3基準のいずれにも該当せず、探索時の未起票判断を維持 / concepts: 同上の `display-state-synchronization.md` 更新に iOS 側の契約と既知の制限 2 件を記録 (review-001 Minor 2 件の申し送り先を蒸留で確定) / deviation なし — S 級・デルタスペックなし。実装フェーズで判明した既知の制限は exploration.md に記録済み → concepts へ反映して回収 / lessons: known-limitation-accepted-without-deviation-record に2例目 (既知の制限の記録先が機構外) を追記 count 1→2、閾値未達 / 検証: iPhone 17 / iOS 26.5 シミュレータで A/B 再現→解消を実測 (verification.md)、review-001 が追加テスト 4 件のミューテーションプローブ 2 回で検出力を実証)。iOS の `replaceSection` / `.full` で header 変更が表示中 supplementary に反映されない問題を `applyFullSnapshot` の条件付き `reloadSections` で解消した S 級変更。
- updated: `core/architecture/display-state-synchronization.md` — 「Section accessory の内容更新」節を新設し、非 null → 非 null は同一行 / supplementary の内容更新・null ↔ 非 null は構造変更という両 OS 共通契約 (Android = DiffCallback の Section H/F 内容比較 + payload rebind `android/ADR-0012`、iOS = `applyFullSnapshot` の header / footer 差分検出 + `reloadSections`) と、View 形式 accessory の等価判定不能に由来する制限 (Android は `KsAnyView` 参照比較で検出 / iOS は `.full` で `.view` 中身差し替えが非反映・`updateAccessory` 経由が正 / iOS `replaceSection` は `.view` が絡む Section を内容不変でも全 Cell 再構成) を記録。内容更新節に Android full 更新の「構造 = DiffUtil / 内容 = commit 後の一括 rebind」を追記し、保証することへ accessory 内容変化の反映を1行追加。timestamp 2026-08-05 に更新。

## 2026-08-05 (3)

- distilled: `fix-android-header-height-refresh` (新規 ADR なし — 候補「`Section.headerHeight` は text accessory にのみ適用し View accessory は Content 高さを優先する」は選別基準 2 / 3 に該当し得るが、iOS が現在 View accessory にも `.absolute` を適用しており「両 platform で text 限定」の決定が未成立のため、今書くと実装と乖離する。Android 内の実装判断 (Text 限定) 単体は DiffCallback 1 行の可逆・局所判断で選別基準の除外に当たり、理由は `isSameHeaderHeight` の KDoc に接地済み。探索時の未起票判断を維持し、`fix-ios-view-header-height-override` で iOS 挙動を確定させる際に両 platform を縛る ADR として起票する / concepts: `core/architecture/display-state-synchronization.md` 更新 (下記 updated 行) / deviation なし — S 級・デルタスペックなし。実装時に決着した論点 (headerHeight 比較を Text accessory 限定とする根拠 = `bindKsAnyView` の `KsAnyView.AndroidView` 分岐が非冪等) は exploration.md に記録、オーナーにも追加乖離なしを確認済み / lessons: visible-appearance-fix-reported-done-without-visual-check に2例目 (テスト green + 独立レビュー APPROVED で完了報告 → オーナー指摘で実機検証を追加実施。「ユニット再現可能だから runtime-behavior-verification の対象外」という判断まで前回と同型) を追記し count 1→2、閾値未達。昇格対象なし (最接近: spike-first 3/5 — kind: success のため閾値は `promote-success` = 5) / 検証: Pixel 6a (Android 16) で修正の有無による A/B と往復を実測 (verification/ に証跡 4 枚と手順)。review-001 / review-002 ともホスト側でミューテーション実測を行い正負両方向の検出力を実証 / 派生起票 2 件: `fix-dsl-header-height-diff` (Compose / SwiftUI 両 DSL が headerHeight 差の差分を生成しない — 実機検証中に発見し iOS 側も同型と確認)、`fix-ios-view-header-height-override` (iOS が View accessory にも headerHeight を適用し移植元・Android と非対称))。header text が同一のまま `Section.headerHeight` だけを変える full 更新が表示へ反映されない問題を、DiffCallback の内容比較拡張で解消した S 級変更。
- updated: `core/architecture/display-state-synchronization.md` — 「Section header の固定高さ」節を新設。固定高さも accessory 同様に再 bind / layout 再評価を経てはじめて反映されること、Android は Text accessory の header に限り内容差として扱うこと (View accessory で高さ差を内容差とすると `KsAnyView` の View が factory から再生成され内部状態を失う)、iOS は supplementary item サイズとして解決し `invalidateLayout()` で追従することを記録。あわせて宣言的 DSL (Compose / SwiftUI) が固定高さ差の差分を生成せず、DSL 経由では表示が更新されない制約を明記。「保証すること」への追加は DSL 経路が未対応で無条件の保証にならないため見送り。timestamp は同日更新のため 2026-08-05 のまま。

## 2026-08-05 (4)

- updated: `core/architecture/declarative-ui-bridge.md` (ksn-concept 経由 — ksn-explore「Store/DSL 非対称性の再発防止」からのハンドオフ) — 「両方式の観測結果対称性」節を新設。DSL 方式だけが持つ検出層 (platform ごとの `DSLDiffCalculator`) の存在理由 (Store は変化が明示される・DSL は再評価差分の翻訳が要る) と無音の失敗の構造、「Store 経由で反映される観測可能な変化は DSL 経由でも同じ表示結果へ到達する (非対応は表示状態同期へ明示記録)」の契約、動的反映プロパティ追加・修正時の対称テスト義務 (core/ADR-0018)、検出層の実装形は platform ごとに自由であることを記録。「保証すること」「してはいけないこと」へ各1項追加。初見可読性レビュー実施 (ADR 参照の宙吊り・「更新経路」の語彙衝突・検出層の存在理由の暗黙化・SettingsRootDiff と「Store 操作」の対応不明・動的反映の未定義等 12 件、ADR 裸参照の正式形維持を除き反映)。timestamp 2026-08-05 に更新。出典: fix-dsl-header-height-diff の探索会話・exploration.md (2026-08-05 コード裏取り)。
- ADR: core/ADR-0018「Store と DSL の更新経路間で観測結果の対称性を対称テストで保証する」を起票し、オーナー承認により accepted (却下案: 文書のみ / レビュー観点のみ / platform 実装形の統一)。

## 2026-08-05 (5)

- updated: `core/architecture/display-state-synchronization.md` (ksn-concept 経由 — fix-dsl-header-height-diff 探索での誤認「ReplaceSection は局所更新」をきっかけにオーナー依頼で概念化) — 「full 更新のコストモデル」節を新設。両 platform とも差分適用基盤 (Android: 平坦 list + DiffUtil / iOS: Diffable snapshot) の上にあり、full 更新は「全行照合 + 差分行のみ再描画」であること、部分更新 API との違いは再描画コストではなく照合コストと変化点の特定有無であること、`replaceSection` は両 platform とも full 経路に合流し「Section 単位だから安い」API ではないこと (Android: `setRootDirect` 合流 / iOS: `applyFullSnapshot` 合流 + `.view` 絡みは追加強制 reload) を記録。「してはいけないこと」へ replaceSection の軽量部分更新扱い禁止を追加、用語表の full 更新定義を「差分だけを再描画する」に改訂。初見可読性レビュー実施 (iOS の内容変化反映との見かけ矛盾・CellRow 内容比較の誤モデル・API 選択規則の欠落・用語ゆれ等 9 分類、すべて反映)。timestamp 2026-08-05 のまま (同日検証)。出典: KsSettingsView.kt applyDiff / KsSettingsViewController.swift applyFullSnapshot / KsCellID.swift のコード裏取り。
- 発見: 「内容更新」節の「full 更新でも同一 ID の Cell の内容変化は取りこぼさない」の iOS 側機構が `applyFullSnapshot` のコードから確認できない (KsCellID は ID のみ・内容変化セルの reconfigure なし)。`fix-ios-full-content-refresh` として簡易起票し、検証結果に応じて本記述を追随させる。

## 2026-08-06

- distilled: `fix-dsl-header-height-diff` (新規 ADR なし — 「preflight で Full に落とす」は可視性 preflight と同型の適用で、対称性の決定は core/ADR-0018 が保持済み。却下案「SettingsRootDiff への headerHeight 専用種別」は exploration.md に記録済みで、concepts の full 更新コストモデルから再導出可能。オーナー承認で見送り確定 / concepts: `core/architecture/display-state-synchronization.md`「Section header の固定高さ」節と `declarative-ui-bridge.md`「両方式の観測結果対称性」節の「DSL 経由では更新されない」記述を解消済みへ追随 (下記 updated 行) / deviation 1 件記録済み (`.replaceCell` を新ツリーで可視な Cell に限定 — 観測結果は spec と同一、verify-002 で Scenario 単位の乖離なし確認)。申し送りの「非表示 Cell への非発行の書き下し」は fix-ios-full-content-refresh が続発機構ごと廃止したため書き下し不要として回収。オーナーにも追加乖離なしを確認済み / lessons: 即時捕捉済みの 2 件 (new-guard-clause-not-checked-against-existing-early-return / scenario-verdict-from-then-observation-skipping-given-when、いずれも本 change を出典に count 1) はカウント規律により増分なし。新規捕捉 new-requirement-not-checked-against-merge-point-contract (spec-review / pain) の 1 例目 (second-opinion-001 Critical: `.full` のみでは当時の `applyFullSnapshot` 契約と両立しない) / 検証: verify-002 VALID (8 Scenario 全一致・ADR-0018 の 4 象限を自動テストで閉鎖)、Android エミュレータ + iOS シミュレータの A/B 証跡 11 枚)。Compose / SwiftUI 両 DSL で headerHeight のみの変更が diff 0 件になる無音の取りこぼしを、可視性と同型の preflight (Full 発行) で解消した M 級変更。ADR-0018 対称テスト義務の初適用例。
- distilled: `fix-ios-full-content-refresh` (新規 ADR なし — proposal 申し送りどおり android/ADR-0012 の対称移植として扱い、オーナー承認で確定。iOS 固有の判断 (具象型変更は `reloadItems` の cell 交換・`reloadSections` 対象 Section は除外) は concepts へ記載 / concepts: `display-state-synchronization.md`「内容更新」節へ iOS 側機構を追記 (下記 updated 行) — 探索で発見された「保証の iOS 側根拠の欠落」を実装とともに回収 / deviation なし — verify-002 で未記録乖離ゼロ確認、オーナーにも追加乖離なしを確認済み / lessons: new-requirement-not-checked-against-merge-point-contract に 2 例目 (second-opinion-001 Major 5 件全採用: reloadSections との identity 矛盾・具象型変更の未定義・既存 preflight 続発との二重 reconfigure 等) を記録し count 2、閾値未達。昇格対象なし。「表示レベル THEN を diff 列観測で代理」の再発 2 例 (本 change verify-001 INVALID と fix-dsl review-001 Minor) は昇格済み test L-001 の再発型としてオーナー裁定により報告のみ / 検証: verify-002 VALID (624 件 / 0 failures)、reconfigure 出口を塞ぐミューテーション probe で新テストの検出力を実測 (争点アサーションのみ failed)。DSL headerHeight preflight の `.replaceCell` 続発は full 更新が内容反映を内包したため廃止 (M4・オーナー裁定 2026-08-06))。iOS の full 更新で同一 ID の表示中セルの内容変化が stale のまま残る欠落を、`FullSnapshotContentTargets` による対象選定 + `reconfigureItems` / `reloadItems` の一括内容再適用 (android/ADR-0012 の対称形) で解消した M 級変更。
- updated: `core/architecture/display-state-synchronization.md` — 「内容更新」節に iOS full 更新の機構 (旧・新 visible projection の双方に存在し値が変わった Cell を `FullSnapshotContentTargets` で選定し `reconfigureItems` で再適用、具象型変更は `reloadItems`、reloadSections 対象は除外、新規挿入・削除・hidden へ重ねない、対象空でも構造反映は実行) を追記し、Android のみだった保証の根拠を両 platform で閉鎖。「full 更新のコストモデル」節の iOS 記述を「内容差は reconfigure が補う」形へ追随。「Section header の固定高さ」節の「DSL 経由では表示が更新されない」を preflight 検出による反映済みの記述へ置換。timestamp 2026-08-06 に更新。
- updated: `core/architecture/declarative-ui-bridge.md` — 「両方式の観測結果対称性」節の既知の非対応の例 (Section header の固定高さ) を、preflight 検出で解消済みの実例として書き換え (表示状態同期への相対リンクで接続)。timestamp 2026-08-06 に更新。
- distilled: `cleanup-comment-lint-debt` (新規 ADR なし — exploration.md の判断 (論点1〜4 はこの change 限りの進め方判断で選別3基準外) を実装後の視点で再確認して維持。deviation の判断 (CI ゲート起票見送り・残債務起票せず) も可逆・局所で基準外 / concepts: `cross/conventions/comment-policy.md` の内容を検証し timestamp を 2026-08-06 に更新 (規約本文の変更なし — 「引用本文の現在形昇格の明確化」「行番号参照の扱いの明記」はオーナー判断で追記見送り) / deviation あり (完備 — スコープ拡大4件・対象外2件・レビューサイクル打ち切り・起票見送り3件、いずれも発生時点でオーナー判断つき記録。archive 前確認で追加乖離なし) / lessons: 捕捉候補3件 (引用昇格パターン impl/pain・対称ファイル突き合わせ impl/success・レビュー収束モデル process/pain) はオーナー判断で捕捉見送り。昇格閾値到達なし)。コメント規約 lint の既存違反 765 件 (215 ファイル) を 0 に返済した S 級変更。機能コード差分 0 をレビュアーが正規化比較で機械的に証明、Android 1986 / iOS 624 tests 全成功。レビュー3周 (上限) 後、オーナー判断で Major 2 件のみ修正して完了

## 2026-08-08

- distilled: `clarify-host-attach-order-contract` (新規 ADR なし — design Decision 1〜3 は core/ADR-0019 (accepted 2026-08-07、index 反映済み) の適用範囲確定として ADR 本文へ反映済み、Decision 4 (契約トリガーは viewDidLoad で window attach と区別) は実装詳細で選別基準外として見送り (コード+テストに接地済み)。ADR 参照コメントは実装・テスト・検証ホストの6箇所に接地済み / concepts: ios/api/ios-native-host.md・android/api/android-native-host.md・core/architecture/native-host-boundary.md を更新 (下記 updated 行) / deviation なし — verify-001 が全 8 Scenario の対応表照合で未記録乖離ゼロを確認 / lessons: spec-scenario-premise-unreachable-in-input-domain に2例目 (相方 spec-review Critical: Root accessory は Store 現在状態から復元不能) を追記し count 1→2、ルール文を「前提の成立可能性のソース照合」へ一般化。新規捕捉2件: change-grade-underestimated-for-cross-capability-contract (spec-review/pain — M→L 昇格の経緯) / fixed-time-wait-instead-of-condition-based (test/pain — 固定時間待機ヘルパを相方 code-review が検出)。昇格対象なし (最接近: spike-first 3/5、kind: success のため閾値 5) / 検証: verify-001 VALID (8 Scenario 全一致・iOS 444 / Android 1990 tests 全成功)、review-001 APPROVED (再確認含む)、E2E 証跡4枚)。「Host 生成 → 構造操作 → 取り付け」の順序で iOS が view load 前の更新を取りこぼす実装ギャップを viewDidLoad の Store pull (`resyncFromStore`) で塞ぎ、view load / attach 時の Store 現在状態復元契約を両 OS 対称のテストで固定した L 級変更。phase-2-maui-core の先行変更 (残件: maui/README.md「既知の制約」の旧記述差し替えは docs-refresh 待ち)。
- updated: `ios/api/ios-native-host.md` — 「view load 時の Store 現在状態からの復元」節を新設。復元対象4種と Root accessory の所有者責務、Store 接続中は Store が正 (view load 前の直接 `applyTheme` / `applyDiff(.full)` は load 時に Store 現在状態で置き換わる — review-001 Suggestion の公開契約化)、収束のみ保証 (Diff のイベント保全なし)、view load と window attach の独立性、Store 非接続 init の従来挙動を記録。「保証すること」へ1項追加。timestamp 2026-08-08 に更新。
- updated: `android/api/android-native-host.md` — detach / 再 attach 復元の段落に初回 attach 前更新の復元と収束の観測境界 (attach 後にメインスレッドのキューが空になった時点、eventual) を追記。「保証すること」へ1項追加。timestamp 2026-08-08 に更新。
- updated: `core/architecture/native-host-boundary.md` — ライフサイクル節へ両 OS 共通の復元契約 (core/ADR-0019) と platform 別の収束境界 (iOS は viewDidLoad 完了時点で同期 / Android は eventual) を追記。「保証すること」へ1項追加。timestamp 2026-08-08 に更新。

## 2026-08-08 (2)

- distilled: `release-host-without-bridge-dispose` (新規 ADR なし — maui/ADR-0007 は実装前ゲートとして accepted 済み (2026-08-07)、実装コードへの ADR 参照コメントも接地済み。root H/F のオーナー裁定 (案A: 現状を仕様として確定 = 所有者が保持し Host 生成のたびに再適用) は core/ADR-0019 が「復元対象外・所有者責務」として既に同一決定を保持しており重複起票せず / concepts: maui/api/native-bridge.md 更新 (下記 updated 行) / deviation 1 件記録済み (root H/F は Host 再生成に引き継がれない — spec 文言非違反で verify-001 は VALID、E2E 証跡で判明しオーナー裁定で仕様確定。申し送りの転記先を蒸留で確定: phase-2-maui-core agenda へ「root H/F 再適用の実装注意」(Android は attach 後に再適用、mapper 直呼びは穴) を追記、既存決定事項「Host 取り付け順序」の追記と併せて受け皿完備。オーナーにも追加乖離なしを確認済み / lessons: spec-silent-visible-change-not-recorded に3例目 (root H/F 消失が doc コメント記録のみ・deviation 未記録のまま完了扱い → review-001 差し戻し) を追記し count 3 で pain 閾値到達、オーナー承認により **process.md L-001 として昇格** (process scope 新設、経緯は details/ へ移動、inbox 削除)。新規捕捉: contract-comment-assertion-not-cross-checked (code-review/pain — ID 契約を断定する doc コメントの矛盾をホストが素通しし相方 second-opinion-002 が検出、count 1)。deinit-teardown-consolidation-breaks-isolation (count 1) は実装フェーズで即時捕捉済み・カウント規律により増分なし / 検証: verify-001 VALID (13 Scenario 全一致・iOS 439 / Android 2008 tests 全成功)、review-001 APPROVED (NEEDS_DISCUSSION からオーナー裁定を経て解消)、E2E 証跡4枚)。Bridge に `releaseHost()` (Host のみ解放・Store 維持・冪等) を追加し、解放後の `makeHost*` が Store 現在状態から復元した新 handle を返す契約へ改めた M 級変更。phase-2-maui-core の先行変更 2 件目 (これで phase-2 実装前の前提変更はすべて完了)。
- updated: `maui/api/native-bridge.md` — lifecycle の保証節を releaseHost 追加後の契約へ全面追随: 「Host だけを解放する経路は存在しない」を削除し、releaseHost の契約 (Store 維持・冪等・旧 handle 購読無効化・取り外しは呼び出し側責務)、解放後 makeHost* の新 handle 復元、root H/F の非復元と所有者再適用 (Android は attach 後、core/ADR-0019)、dispose と releaseHost の非対称が意図的であること、Context 制約の緩和 (旧「Bridge の寿命は Host の Context の寿命を超えてはならない」→ releaseHost 後は Context 参照なし) を記録。ADR 参照範囲を 0001〜0007 へ更新。timestamp 2026-08-08 に更新。

## 2026-08-08 (3)

- distilled: `add-maui-core` (ADR: maui/ADR-0009 (net10.0 TFM + gateway seam) を design.md Decision 1 から起票しオーナー承認で **accepted**、実装コード 2 箇所 (IKsSettingsGateway.cs / KsSettingsView.Maui.csproj) へ参照コメント接地。design Decision 2〜5 は agenda 決定の詳細化・可逆判断のため昇格せず / concepts: `maui/api/maui-facade.md` **新規** (下記)、`maui/api/native-bridge.md` に facade への関連リンク追加 / deviation 7件 (spec 沈黙領域の実装判断。verify-001 で未記録乖離ゼロを確認済み) / lessons: 新規捕捉2件 — `e2e-in-impl-wave-caught-handler-platform-bugs` (impl/success: 実装ウェーブ内 E2E がユニットテスト非到達の実バグ2件を捕捉、count 1)、`design-procedure-order-not-cross-checked` (code-review/pain: design 明記の手順順序と実装順序の照合漏れをホストが素通しし相方が Major 検出、count 1)。閾値到達なし・昇格なし / 検証: review-001 APPROVED・second-opinion-002 採用3件で修正サイクル1周・review-002 APPROVED・verify-001 VALID (15 Requirements / 29 Scenarios)・両 OS E2E 全項目 PASS / 申し送り: Theme・Section.IsVisible・DataTemplateSelector・review Suggestion 6件 → phase-4 agenda へ、NU1608/NU1107 → パッケージング着手時 (見送り合意)、roadmap phase-2 を completed に反映)。`KsSettingsView.Maui` (facade 層) を新設し LabelCell で XAML→Bridge→Native 経路を動作証明した L 級変更。phase-2-maui-core 完了。
- new: `maui/api/maui-facade.md` — MAUI facade の公開契約 (公開 API の形・更新の意味論 (構造即時 / 内容バッチ / UI スレッド契約)・lifecycle (切断中も Store へ流し続け再接続で復元・root H/F は facade 所有で attach 後再適用)・制約 (logical tree 外・DataTemplateSelector 非対応・重複配置例外)) を記録。初見可読性レビュー実施済み (指摘4件反映: 読み順誘導・A 分類とAiForms の注釈・phase 識別子の一般化)。timestamp 2026-08-08。
- updated: `maui/api/native-bridge.md` — 関連節に maui-facade.md へのリンクを追加 (facade 層の新設に伴う接続のみ、契約内容の変更なし)。timestamp 2026-08-08 のまま (同日検証済み)。

## 2026-08-09

- distilled: `add-maui-samples-foundation` (ADR: maui/ADR-0010 (AndroidX 版競合の binding 層吸収) を実装実証の帰結 (restore 実証 + mutation probe で宣言が load-bearing・10.0.70 でも前提不変・NuGet 経路のみ未検証) を Consequences へ追記して **accepted** に昇格、index 更新。参照コメントは Binding.Android.csproj に接地済み。落とした候補3件 — ReactiveProperty.Core 採用 (サンプル局所・full 版への切替は非破壊)・MAUI 10.0.70 統一 (バージョンピンの追随判断)・MAUI パッケージ明示宣言 (csproj 局所判断) はいずれも可逆・局所で選別基準外、proposal / deviation.md の記録で十分 / concepts: `cross/conventions/sample-parity.md`・`maui/api/maui-facade.md` 更新 (下記 updated 行)。新規概念なし (SampleScreen descriptor はサンプル内部構造で価値 lint 対象外) / deviation 3件記録済み (MAUI パッケージ明示宣言・10.0.70 統一・MauiHost 随伴修正 — いずれも spec 沈黙領域のオーナー指示。verify-001 で未記録乖離ゼロ確認、オーナーにも追加乖離なしを確認済み) / lessons: 即時捕捉3件 — 新規 readme-convention-scope-misapplied-to-sample-artifacts (spec-review/pain: 相方 Major の docs-refresh 規約適用をオーナーが「新設サンプル付属 README は対象外」と却下、count 1)・labelcell-hint-value-overlap-flagged-as-defect (code-review/pain: Hint/Value の重なりは仕様どおりとオーナー却下、count 1)、change-grade-underestimated-for-cross-capability-contract へ境界観測を追記 (決定済み・低リスクの横断はオーナー裁定で M があり得る、count 増分なし)。補完なし・昇格閾値到達なし (最接近: spike-first success 3/5) / 検証: verify-001 VALID (2 capability・6 Scenario 全一致、mutation probe で回帰検出力実証、修正後再検証込み)・review-001 APPROVED (Minor 3件すべて解消)・second-opinion-001 spec-review 採用4/降格3/却下1・second-opinion-002 code-review APPROVED (指摘0)・Maui.Tests 115件全成功・両 OS シミュレータ/エミュレータ起動確認 / 申し送り: ルート README「未着手」表記 → docs-refresh 待ち、review Suggestion 2・3 と Compatibility 要否 → phase-4 agenda TODO、NuGet 経路の効果検証 → パッケージング着手時。roadmap phase-3 を completed に反映)。`samples/maui/` に MAUI サンプルアプリ土台 (デモ一覧 + SampleScreen 一元定義 + LabelCell 検証ページ、ReactiveProperty.Core) を新設し、AndroidX Lifecycle 版競合を binding 層で吸収した M 級変更。phase-3-samples-foundation 完了。
- updated: `cross/conventions/sample-parity.md` — 「保証すること」冒頭の対象注記から「samples/maui は未実装 — README のみ」を解消し、対象を `samples/ios` / `samples/android` / `samples/maui` の3つへ。MAUI Sample は土台と検証枠「LabelCell 検証」のみでデモ画面未追随 (「実装順序による一時的な片側先行」に該当・後続 change の tasks で追跡中) と追随。timestamp 2026-08-09 に更新。
- updated: `maui/api/maui-facade.md` — 「現時点の範囲」の配布注記を書き分け: AndroidX Lifecycle 版競合 (NU1608 / NU1107) は ProjectReference 経路では binding 層の明示宣言で解消済み (maui/ADR-0010)・利用側にピンや `NoWarn` は不要、NuGet パッケージ経路への効果は未検証のまま。timestamp 2026-08-09 に更新。

## 2026-08-09 (2)

- distilled: `harden-update-accessory-unknown-id` (ADR: core/ADR-0020 はフロー内で accepted 済み (2026-08-09)・index 反映済み・両 OS Store 実装へ参照コメント接地済みのため蒸留時の新規起票なし。却下案 (案B: Host 側 missing ID 処理の warn 化) は ADR-0020 Alternatives に記録済み / concepts: 3件更新 (下記 updated 行) / deviation なし — verify-001 が対応表照合で未記録乖離ゼロを確認、オーナーにも追加乖離なしを確認済み / lessons: counterpart-approved-not-proof に3例目 (second-opinion-002 APPROVED 指摘0 の同サイクルでホスト review-001 が Major 1 / Minor 2 を検出) を追記し count 3 で pain 閾値到達、オーナー承認により **process.md L-002 として昇格** (経緯は details/counterpart-approved-not-proof.md へ、inbox 削除)。新規捕捉2件: old-contract-comments-in-other-layers-not-swept (impl/pain — MAUI 層3箇所の旧契約コメント掃き残し、count 1) / symmetric-test-case-added-without-regression-detection (test/pain — Android 契約表2ケースの観測点空振り、count 1)。change-grade-underestimated へ境界観測を追記 (単一契約の platform 投影・未決ゼロは明記対応で M 維持、count 増分なし) / 検証: verify-001 VALID (3 spec・16 Scenario 全一致・iOS 646 / Android 2024 tests 全成功)、review-002 APPROVED、second-opinion-001 spec-review 全7件採用・second-opinion-002 code-review APPROVED (指摘0) / 申し送り: Robolectric 待機ヘルパの共通化は `changes/consolidate-robolectric-wait-helpers` として別セッションで進行中 (本 change では扱わない)、Android storeCollectJob 沈黙経路の Host 側防御は将来必要時に別途起票 (exploration で見送り確定)、strictMode 前提アサーション1行は任意 (review-002 Suggestion・非対応で完了)。roadmap 由来ではない (proposal に roadmap 欄なし) が、phase-4-basic-input-cells の agenda 論点「updateAccessory の未知 sectionID 契約 (本フェーズ前に判断)」の解消そのものであるため、当該論点を決定事項へ移動して実装完了 (案A 採用・core/ADR-0020・archive 参照) を反映済み)。「canonical UUID だが未知の sectionID」の `updateAccessory` が state 更新 no-op のまま Diff だけ無条件発行され Host の missing ID 処理 (iOS DEBUG assert / Android strictMode 例外・沈黙経路) に到達する欠落を、「state 更新が成立しない構造 Diff は発行しない」ガード (core/ADR-0020) の適用拡大で解消した M 級変更。
- updated: `core/architecture/store-and-update-streams.md` — 更新契約に「state 更新が成立しなかった構造操作は Diff を発行しない (core/ADR-0020)」の段落を新設 (未知 sectionID の `updateAccessory` section 系 target は状態・Diff 両ストリーム無発行の no-op、Root 系 target は対象外、保証がないと Host の missing ID 検出に到達する根拠つき)。「保証すること」へ Store state と発行 Diff の一致を1項追加。timestamp 2026-08-09 に更新。
- updated: `core/core-model/structural-changes.md` — Accessory 更新節の「Section target の ID が存在することは呼び出し側の事前条件」を、Store が両 OS 対称に保証する no-op (core/ADR-0020) の記述へ置換 (Root target は判定対象外)。timestamp 2026-08-09 に更新。
- updated: `maui/api/native-bridge.md` — ID の interop 契約節の「`updateAccessory` はこの契約の対象外で、未知の sectionID でも Store が更新通知を発行する」を、section 系 target への no-op 契約適用 + Root 系 target の判定対象外 (`sectionID` 引数を参照しない) へ置換 — 変更後契約の正反対だった記述 (second-opinion-001 Major 指摘の必須追随先) を解消。timestamp 2026-08-09 に更新。

## 2026-08-09 (3)

- distilled: `consolidate-robolectric-wait-helpers` / `unify-inline-looper-idle-calls` (連続する2つの S 級変更をまとめて蒸留。実装は共有ヘルパファイルを双方が触るためファイル単位で分割できず、1コミット f67c6c2 として記録済み) (ADR: 両変更とも exploration の ADR 候補欄は「なし」。実装を経た視点で選別3基準に再度照らしてもテスト専用・完全可逆・capability 境界内であり該当なし、新規起票なし / concepts: 1件更新 (下記 updated 行)。共有ヘルパ `KsSettingsViewTestSupport.kt` の存在自体は同パッケージを見れば再導出でき、テストの増減で腐るため価値 lint により concepts へ含めない / deviation なし — 両変更ともオーナー指示による逸脱は発生していない / lessons: 新規捕捉1件 exhaustiveness-judged-by-single-search-axis (impl/pain — 網羅性の判定を単一検索軸で行って取りこぼし、独立2 change で再現し count 2。pain 閾値 3 に未達のため inbox 保持)。既存 old-contract-comments-in-other-layers-not-swept との match を検討したが、あちらは「掃く範囲 (層) が狭い」型、今回は「範囲は正しいが検索式の形が記法バリエーションを覆わない」型で別パターンと判定。テストヘルパ複製そのものは共有ヘルパの新設で構造的に解消したため捕捉せず (再発時に起票) / 検証: consolidate は review-002 APPROVED (Minor 1・Suggestion 1 を修正、レビュアーが一時プローブでタイムアウト経路を実測)、unify は review-001 CHANGES_REQUESTED (完全修飾形の残存 1 箇所) → 修正 → review-002 APPROVED (新規指摘 0、独立3軸の交差検証で網羅性を確認)。Android 全件 2024 tests / 0 failures、comment-policy-lint 禁止 0 件。S 級のため verify は非適用、second-opinion は config の対象級 (m/l) 外 / 申し送り: `layoutAndDraw` ほか ui テストに残る重複ヘルパ (HostActivity 9ファイル・layoutSettingsView 4ファイル等) の棚卸しは後続タスクとして起票済み。unify の exploration が除外理由に挙げた「単一ファイル内でしか使われず重複していない」は `layoutAndDraw` について事実と異なることが review-001 Suggestion で判明しており、後続へ申し送り済み)。ui テスト各ファイルに複製されていた Robolectric の待機・観測ヘルパを `KsSettingsViewTestSupport.kt` へ集約し、インライン `shadowOf(Looper.getMainLooper()).idle()` 27箇所と名前違いの重複 `awaitDifferCommit` も同ファイルへ寄せた S 級変更 2 本。テストコードのみで production は無変更 (+198 / −412 行)。
- updated: `cross/conventions/test-execution.md` — Android の実行件数確認パスを実測値へ訂正 (`build/test-results/<variant>/` → `testDebugUnitTest/`、release は `testReleaseUnitTest/`)。ディレクトリ名が variant 名 (`debug` / `release`) ではなく**タスク名**である旨を明示した。`<variant>` を `debugUnitTest` 等と読み替えると集計対象が 0 件になり、実際に実装ワーカーが「テスト 0 件」を踏んで報告している。timestamp 2026-08-09 に更新。

## 2026-08-11

- distilled: colorPrimary 宣言元参照への是正 (S 級・change ディレクトリなし。コミット 1b6c0db) (ADR: **android/ADR-0013 を新規起票し accepted** — 「リソース参照は宣言元ライブラリの R クラス経由で行う」。選別基準 2 (Android UI モジュールと MAUI Binding の依存管理という境界をまたぐ) と 3 (material 版引き上げの可否という将来の決定を制約する) で該当と判定。Alternatives には実際に採っていた回避策 (material 1.12.x への版固定継続) と却下理由を記録。参照コメントは EffectiveStyle.kt と Binding.Android.csproj に接地済み / concepts: **変更なし**。`core/styling/style-resolution.md` の ButtonCell title 色の記述は「Cell 固有の意味値は CellStyle より先に解決する」という抽象レベルで参照先ライブラリに触れておらず、今回の変更は概念記述に影響しない。`concepts/android/` への conventions カテゴリ新設は rules.md の新設条件 (収まらない概念が3つ以上蓄積) に未達のため見送り、知識の受け皿は ADR-0013 とした / deviation なし — オーナー指示による逸脱は発生していない / lessons: exhaustiveness-judged-by-single-search-axis に3例目 (行折り返しで分断された語句を行単位 grep が原理的に拾えず掃き残し、review-002 が検出) を追記して count 3 で pain 閾値到達、オーナー承認により **impl.md L-003 として昇格** (経緯は details/exhaustiveness-judged-by-single-search-axis.md へ、inbox 削除)。新規捕捉1件: build-tree-shared-by-orchestrator-and-worker (process/pain — レビューワーカーにテスト実行を委譲中にオーケストレーターも同じ Gradle ツリーへ clean と --stop を実行し、双方 BUILD FAILED。変更起因と区別のつかない偽の失敗で切り分けに2往復、count 1) / 検証: review-003 APPROVED (3周。review-001 Minor 1 → 同一ファイル内の帰属矛盾、review-002 Minor 1 → 行折り返しで残った Theme.kt、review-003 指摘0)。Android は clean 後の単独実行で 2164 tests / 0 failures、comment-policy-lint 禁止 0 件。MAUI は Android 15 エミュレータへデプロイし共通フィールド統合デモの ButtonCell「ログアウト」がテーマの colorPrimary (#6750A4) に解決され SYSTEM_BLUE へ落ちないことをピクセル実測。S 級のため verify 非適用、second-opinion も config の対象級 (m/l) 外 / 申し送り: docs/cells.md:134 に同じ「Material colorPrimary」表現が残るが docs/ は docs-refresh 経由でのみ書き換えるため次回の対象候補。Xamarin.Google.Android.Material 1.12.0.5 の版固定解除は別途の互換検証が必要で本変更では扱わない (ADR-0013 Consequences に保留として記載))。material 1.13+ の非推移的 R.txt で壊れる `com.google.android.material.R.attr.colorPrimary` 参照を、宣言元の `androidx.appcompat.R.attr.colorPrimary` へ是正した S 級変更。aar 実測で是正対象が本1箇所に閉じること (他の material R 参照 attr 4 件 / id 25 件 / dimen 2 件はすべて material 自身の宣言) を確認済み。

## 2026-08-11 (2)

- distilled: `add-maui-basic-input-cells` (ADR: **maui/ADR-0011・0012・0013 を実装後視点で更新して accepted に昇格** (0011: 共通基底 DTO `KsBridgeCell` と source-breaking 許容を追記 / 0012: 書き戻し正規10プロパティ・SelectedIndices の集合等価・ISO パース失敗の既定値構築・android/ADR-0014 との関係を追記 / 0013: 変更なし)。**maui/ADR-0015 (IconSource の image source service 非同期実体化) を新規起票し accepted** — design Decision 7 由来、選別基準 2 (MAUI 画像 pipeline → native 輸送の境界越え) と 3 (phase-5/6 の MauiView 実体化の先例)。落とした候補 — design Decision 2 (ADR-0003 の具体化。native-bridge.md の操作通知節へ反映して回収)・Decision 4/5/8/10 (局所的 API 詳細)・Decision 9 の cellId 温存 (native-bridge.md へ追記)・パスワードマスク修正 (単発バグ修正)・Section.HeaderHeight (additive で自明)。参照コメントは KsSettingsController (ADR-0015) に追加、0011〜0013 は実装時に接地済み / concepts: maui/api/maui-facade.md 全面改訂・maui/api/native-bridge.md 大幅更新 (下記 updated 行、初見可読性レビュー実施・指摘反映済み)、cross/conventions/sample-parity.md へ agenda 論点8 の例外条項を追加 / deviation 5件記録済み (ニックネーム callback・Android IME フォーカス喪失 (別 change で解消済み)・パスワードマスクのスコープ追加・アイコン色形状差・Section.HeaderHeight 追加)。オーナーに追加乖離なしを確認済み / lessons: review-recommended-fix-treated-as-hypothesis に3例目 (BG8605 の @JvmSynthetic 推奨を実装者が実測反証、review-002 が自らの誤り認定) を追記 count 3 (success 閾値5未達)。parity-target-sample-api-not-cross-checked は3例とも記録済み (同一 change のため count 1 のまま) / 検証: verify-001 VALID (但し書き: 7.2 実機 IME は fix-maui-entrycell-focus-loss で解消)・review-001 CHANGES_REQUESTED (Major 2 / Minor 7) → 全解消 → review-002 APPROVED・second-opinion spec 2回 + code 1回。MAUI 237 / iOS 711 / Android 2164 全緑 / 申し送り: EntryCell 値変更通知の公開はオーナー判断で**見送り確定** (AiForms 原典にも無く TwoWay で足りる)・icon リース破棄時機 (review-002 Minor-10 + 保留b) → `fix-maui-icon-lease-disposal-ordering` スタブ起票・compose flaky テスト → `fix-compose-dsl-double-update-flaky-test` スタブ起票・samples/maui README の未追随 → docs-refresh 待ち。roadmap phase-4 を completed に反映)。MAUI facade に CellBase 派生11種 + 双方向バインド10プロパティ + interaction delegate/listener (コールバック方向 interop 初) + Theme/CellStyle 公開 + IconSource 実体化 + サンプル4ページを追加した L 級変更。phase-4-basic-input-cells 完了。
- distilled: `fix-maui-entrycell-focus-loss` (ADR: maui/ADR-0014 は実装フェーズで起票・accepted 済みのため蒸留時の新規作業なし。参照コメントを SettingsViewHandler.cs / KsFillMeasure.cs へ追加 / concepts: maui/api/maui-facade.md へ「配置の制約 (Android の measure 契約)」節を追加 (大きさが決まる配置を使う・内容サイズを問われる配置は非推奨) / deviation なし / lessons: 新規捕捉2件 — exploration-open-point-settled-silently-by-impl (impl/pain: 未決論点「無限制約の扱い」を実装が 0 即答で暗黙確定し公開挙動の後退、review-001 Major-1 が差し戻し、count 1)・device-evidence-left-in-session-scratchpad (impl/pain: 実機証跡が scratchpad のみで change 配下に無し、review-001 Major-2 が差し戻し、count 1) / 検証: review-001 CHANGES_REQUESTED (Major 2 / Minor 2) → 修正 (無限制約は base.GetDesiredSize フォールバック・KsFillMeasure 切り出し + テスト11件・証跡移送 + evidence.md) → review-002 APPROVED (ミューテーション3種で回帰検出力実測)。Pixel 6a 実機 A/B で `.F` フラグ維持・幅ゼロ化なしを確認 / 申し送り: なし)。MAUI Android の EntryCell が打鍵ごとにフォーカスを失う不具合を、handler 層の measure 契約 (有限制約は fill 即答・非有限はフォールバック) で解消した S 級変更。
- distilled: `fix-entrycell-writeback-caret-race` (ADR: **android/ADR-0014 (フォーカス中の EntryCell 入力欄は値の SSoT) を新規起票し accepted** — proposal の申し送りどおり ADR-0001 の補完として起票し、ADR-0001 が却下した一方向化との違い・「高速入力の取りこぼし解消」の適用範囲限定・review-001 Minor-1 の負の帰結 (書き戻さない利用構成では blur 時に最後の bind 値へ戻る) を Consequences に記録。案B (世代トークン)・案C (C# エコー抑止)・案D (キャレット補正) は Alternatives に却下理由つきで記録。参照コメントを EntryCellViewHolder.kt へ追加 / concepts: android/api/android-native-host.md の保証節と core/cells/input-cells.md のプラットフォーム差へ SSoT 契約を追記 (review-001 Minor-1 の申し送りを回収) / deviation なし (review-001 が無断逸脱なしを確認) / lessons: robolectric-green-masked-real-ime-key-delivery に3例目 (レースは実機のみで自然再現・実機 A/B を完了条件化) を追記 count 3 (success 閾値5未達)。device-harness-defect-indistinguishable-from-fix-failure はフロー内で捕捉済み (count 1) / 検証: review-001 APPROVED (ミューテーション2種で検出力実測・2188 tests 全緑)・second-opinion code APPROVED (指摘0)・実機 A/B: native 10/18 破損 → 0/17、MAUI 7/18 → 0/18、IME composing 維持・入力継続・focus-loss 再発なし / 申し送り: iOS 同型レース調査 → `fix-ios-entrycell-writeback-race` スタブ起票。Gboard 以外の IME・MAUI の KEYCODE_MOVE_END スクロール挙動は見送り)。EntryCell の書き戻しレース (文字欠落・並び替え・IME desync) を bind 側のフォーカス期間ガード + blur 再同期で解消した M 級変更。
- updated: `maui/api/maui-facade.md` — 全面改訂: Cell 12種・MAUI 慣例型と DatePickerUIStyle・PickerCell の選択導出・スタイル公開面・Section.IsVisible / HeaderHeight・DataTemplateSelector 対応 (制約解除)・「ユーザー操作と双方向バインド」節 (TwoWay 10プロパティ・Radio group 同期・Command 実効有効・EntryCell は TwoWay のみ)・IconSource の非同期解決・「配置の制約」節 (maui/ADR-0014) を追加。「現時点の範囲」を後続フェーズ基準へ更新。初見可読性レビューの指摘 (経路図の Binding assembly 明示・TemplateStartIndex 説明・バッチ/単発の観察一本化ほか) を反映。timestamp 2026-08-11。
- updated: `maui/api/native-bridge.md` — per-type DTO と共通基底 `KsBridgeCell`・双方向値の輸送規約・`KsBridgeSection.isVisible` / `headerHeight`・replaceSection の cellId 温存・「ユーザー操作通知」節 (単一チャネル・種別メソッド・同期呼び出し・寿命) を追加。責務境界表へ gateway 行を追加。現時点の範囲を Cell 12種へ更新。初見可読性レビューの指摘 (ID 有効性の言い直し・未定義語の平文化ほか) を反映。timestamp 2026-08-11。
- updated: `android/api/android-native-host.md` — 保証することへ「フォーカス中の EntryCell 入力欄は値の SSoT」(android/ADR-0014) と利用側契約 (callback を受けて cell.text を更新する) を追記。timestamp 2026-08-11。
- updated: `core/cells/input-cells.md` — プラットフォーム差へ Android EntryCell の SSoT 契約 (iOS は未検証・未導入) を追記。timestamp 2026-08-11。
- updated: `cross/conventions/sample-parity.md` — 例外節「デモ対象の公開 API が存在しない platform」を新設 (phase-4 agenda 論点8 の決定。Store/DSL デモは native のみ・MAUI 専用デモの逆方向も許容)。「保証すること」冒頭の対象注記を「未追随は CustomCellDemo のみ (phase-5 で追随予定)」へ更新。timestamp 2026-08-11。

## 2026-08-11

- distilled: align-view-accessory-header-height (core/ADR-0021 accepted 起票 / styling/list-appearance.md 更新 — Header 高さ解決の accessory 種別非依存・clip・hosted view の領域占有を明文化)

## 2026-08-12

- distilled: add-maui-accessory-views (maui/ADR-0016〜0018 を実装結果で更新し accepted 化)
- updated: `maui/api/maui-facade.md` — header/footer View の公開 API (View 優先・text フォールバック)・更新セマンティクス (参照が正・内容は live・サイズ追従)・logical tree 接続と BindingContext 継承 (Section/Cell との違い)・多重配置検出への accessory View の追加と未参加 Section の非奪取・lifecycle 復元 (facade 所有 + Host 世代 wrapper)・現時点の範囲から任意 View を削除。timestamp 2026-08-12。
- updated: `maui/api/native-bridge.md` — 更新 API へ `updateAccessoryView` / `invalidateAccessoryMeasurement` を追加 (1:1 原則からの意図的な一歩)、`KsBridgeSection` の headerView/footerView 輸送と view 優先、現時点の範囲の accessory 制限を解除。timestamp 2026-08-12。
- updated: `core/architecture/store-and-update-streams.md` — 一過性通知の表と更新契約へ `invalidateAccessoryMeasurement` (復元可能状態を変えない再計測要求) を追加。timestamp 2026-08-12。
- updated: `cross/conventions/sample-parity.md` — 例外「デモ対象の公開 API が存在しない platform」へ適用拡張 (主対象が platform 固有の意味論ならページ全体を固有画面にできる。オーナー裁定 2026-08-11、AccessoryViewsDemoPage) と「MAUI 固有」区分の区別を明文化。timestamp 2026-08-12。
- created: `maui/architecture/view-materialization.md` — MauiView 実体化機構の共有基盤 (seam 契約・自己計測 wrapper・論理所有と lease の寿命分離・退役順序・再利用規律)。maui に architecture/ カテゴリを新設 (rules.md 更新、オーナー合意)。初見可読性レビューの指摘 10 件 (未定義語アンカー・CustomCell の時制・ItemContentView の継承誤読ほか) を反映。timestamp 2026-08-12。

## 2026-08-12 (2)

- distilled: add-maui-custom-cell (maui/ADR-0019〜0021 accepted / view-materialization.md・maui-facade.md・native-bridge.md・sample-parity.md 更新 / lessons spec-review L-001 昇格)
- updated: `maui/architecture/view-materialization.md` — 利用者を accessory View + CustomCell.Content の2つへ更新 (「将来 phase-5」の時制を解消)。controller の cell 単位所有表、cell content の値確定前検査 (`IKsCellContentGuard` と例外型)、「cell content の世代トークン」節 (参照遷移のたびに振り直す / 同一トークン間は埋め込みインスタンスが安定)、退役順序へ ReleaseHost の内容なし世代再発行と `ReplaceCells` 1バッチ必須を追加。「native への埋め込みの継ぎ目」節を新設 (iOS の行ごとの入れ物と引き取り規則 / Android の `key(token)` とタッチ遮りの返還・native 利用者が content に `AndroidView` を置く場合の既知の制約)。「サイズ変化の伝播」節で accessory の `invalidateAccessoryMeasurement` と cell content の self-sizing 完結を対比。timestamp 2026-08-12。
- updated: `maui/api/maui-facade.md` — Cell 階層を 13 種へ更新し「CustomCell (任意の View を行の内容にする)」節を新設 (Content / Command / Tapped / ShowArrowIndicator、タップの棲み分けと二重発火なし、live 更新と差し替えのみ再発行、silent no-op、値確定前の多重配置検査、ItemTemplate 生成行の独立、提供層は①②のみ)。更新の意味論・logical tree 接続・多重配置・lifecycle 復元へ Content を追加。現時点の範囲から CustomCell を外し、利用者定義 Cell 登録機構・ContentTemplate・仮想化を未提供として明示。timestamp 2026-08-12。
- updated: `maui/api/native-bridge.md` — per-type DTO を 13 種へ更新し `KsBridgeCustomCell` (view / contentToken / showArrowIndicator / hasTapHandler、基底の共通スロットは写さない) を追加。操作通知へタップ専用メソッド (`customCellTapped` ほか) と `hasTapHandler` による onTap 装着の条件を追記。現時点の範囲を Cell 13 種へ更新。timestamp 2026-08-12。
- updated: `cross/conventions/sample-parity.md` — CustomCellDemo の MAUI 未追随の注記を実態 (パリティ対象は全 platform 揃い) へ更新し、platform 固有画面の例へ `CustomCellMauiSpecificDemoPage` (パリティ画面と MAUI 固有デモの分離) を追加。timestamp 2026-08-12。

## 2026-08-13

- distilled: fix-ios-replace-cell-type-change (ADR なし / display-state-synchronization.md・ios-native-host.md 更新 / lessons test scope 捕捉1件)
- updated: `core/architecture/display-state-synchronization.md` — 「内容更新」節へ、iOS 部分更新経路 (`replaceCell` 単発・`replaceCells` バッチ) が full 経路と同じ具象型比較で型変化 Cell を `reloadItems` の cell 交換へ振り分けることを追記 (型変化を reconfigure に流すと reuse identifier 不一致でクラッシュ)。timestamp 2026-08-13。
- updated: `ios/api/ios-native-host.md` — `replaceCell` / `replaceCells` が同一 ID のまま具象型が変わる差し替えを受け付け、Native cell の交換で反映されることを利用契約に追記。timestamp 2026-08-13。
- updated: `maui/architecture/view-materialization.md` — 「native への埋め込みの継ぎ目」節へ「Android に iOS のような行ごとの入れ物が無い理由」を追記。iOS で実機確定したリサイクル時レース (退役行の遅延後片付けが表示中の行から共有 view を剥がして空行化) の Android 同型経路を構造検証した結果: compose-ui 1.7.5 の破棄チェーン (`layoutNode.onDetach` の `removeAllViewsInLayout` / `onRelease` / `removeAndroidView`) はすべて親側・自己スコープで子側 detach が存在せず、子側操作 (factory の detach・wrapper 構築時の `RemoveFromParent`) は表示方向のみ、reuse 経路は `onReset` 非指定 (`ComposeNode`) で不発。エミュレータ実証 (API 35、高速フリック往復 2 セッション計約 600 フリック・約 100 検査点) で空行 0 件・例外 0 件。Compose BOM メジャー更新時の再確認注意を併記。timestamp 2026-08-13。(構造リスク検証セッション、変更フロー非経由の ksn-concept 直接更新)
- distilled: align-maui-accessory-placement-guard (maui/ADR-0022 起票・accepted / maui/api/maui-facade.md の配置制約を値確定前検査・バッチ全件検査・非ロールバック+再収束の契約へ更新)
- distilled: fix-maui-custom-cell-content-guard-leak (ADR なし — maui/ADR-0022 既記載の「guard は弱参照」を ContentGuard 側へ適用し切った単発修正)
- updated: `maui/api/maui-facade.md` — lifecycle の弱参照保証を「コレクション・Cell への購読」から「model から SettingsView 側へ向かう内部参照すべて (配置検査の尋ね先 guard を含む)」へ拡大。Content 設定済み CustomCell の外部保持も回収を妨げないことを明記。timestamp 2026-08-13。

## 2026-08-16

- distilled: perf-android-customcell-composition-reuse (core/ADR-0022 accepted + core/ADR-0015 superseded 化・android/ADR-0015 accepted / custom-cell.md・view-materialization.md 更新 / lessons: process L-004 昇格・impl pain 捕捉 1 件 / deviation 2 件)
- updated: `core/cells/custom-cell.md` — 適用除外の ADR 参照を core/ADR-0022 (supersede 先) へ追随。してはいけないことへ「行の再表示をまたぐ content 内部の一時状態 (remember / @State) の保持・初期化を仮定しない」を追加 (Android は android/ADR-0015 のリサイクルで維持されることがあり、iOS は ios/ADR-0002 で再生成)。timestamp 2026-08-16。
- updated: `maui/architecture/view-materialization.md` — 「Android に行ごとの入れ物が無い理由」の reuse 経路の根拠へ compose-runtime の非 reusable ノード強制置換 (onReset なしは再利用対象外) を明記し、android/ADR-0015 (ReusableContent 導入) 後も前提不変であることを追記。deactivate 経路 (`AndroidViewHolder.onDeactivate`) がプール投入時に新たに走るようになったことと、Bridge 専用回帰テスト + 実機高速フリック再検証 (2026-08-16) による裏取りを追記。timestamp 2026-08-16。

## 2026-08-19

- distilled: add-accessory-visibility-toggle (core/ADR-0023 accepted / settings-tree.md・list-appearance.md・maui-facade.md・ios-swiftui.md・android-compose.md 更新 / lessons: impl L-004 昇格 / deviation 2 件)
- updated: `core/core-model/settings-tree.md` — Section の表示トグル `isHeaderVisible` / `isFooterVisible` (既定 true、値等価性に参加、非表示中も accessory を保持) と「内容不在 (未設定/空 text) なら高さ指定でも領域を作らない」保証を追記。timestamp 2026-08-19。
- updated: `core/styling/list-appearance.md` — Header / Footer の表示判定を「可視トグル && 内容あり」の AND (内容不在 = 未設定/空 text で両 OS 共通、view accessory は常に内容あり) とし、高さ解決は存在判定の後で存在を作らないこと (core/ADR-0023) を明記。timestamp 2026-08-19。
- updated: `maui/api/maui-facade.md` — `Section.IsHeaderVisible` / `IsFooterVisible` を公開 API へ追記し、「現時点の範囲」の未提供リストから表示トグルを削除。timestamp 2026-08-19。
- updated: `ios/api/ios-swiftui.md` / `android/api/android-compose.md` — Section 構築 API の受け取り引数へ表示トグル `isHeaderVisible` / `isFooterVisible` を追随。timestamp 2026-08-19。

## 2026-08-20

- distilled: implement-modern-style (ADR 新規なし — ios/ADR-0003 は探索時に accepted 済み、「Theme 4属性の置き場所」候補は design.md の判断どおり見送り [core/ADR-0009 の文法適用・可逆で局所的、再提案抑止は style-resolution.md に記載] / list-appearance.md 大規模改訂・style-resolution.md 追記・ios-native-host.md / android-native-host.md 追随 / lessons: impl pain 捕捉 2 件 [spec-review 1 件は実装中に捕捉済み] / deviation 6 件)
- updated: `core/styling/list-appearance.md` — Modern の契約を全面改訂: iOS は `.insetGrouped` を廃した自前 Section 装飾 (ios/ADR-0003)、Theme の Section 装飾4属性と style 別 platform 既定への解決、`sectionMargin` の Section 単位意味論 (H/F 行にも水平成分適用・Root H/F の内側に list 端余白)、箱は Cell 行のみで H/F は外側 (両 OS 共通)、箱と Cell 背景の合成契約、Modern separator の左右対称 inset、長い Section の箱端規則。「生の margin・radius 値を統一しない」を「既定値を統一しない」趣旨へ改訂 (Android spec の H/F 水平 inset SHALL NOT は deviation でオーナーが上書き済み — deviation 後の契約を正として記載)。timestamp 2026-08-20。
- updated: `core/styling/style-resolution.md` — 特殊な解決規則へ Section 装飾4属性の2段解決 (CellStyle 段なし、Theme → style 別 platform default) と「集約中間型 (`SectionStyle` 等) を作らない」を追記。timestamp 2026-08-20。
- updated: `ios/api/ios-native-host.md` — `.modern` = `.insetGrouped` の陳腐化記述を自前 Section 装飾 (ios/ADR-0003) へ追随。
- updated: `android/api/android-native-host.md` — Modern のハードコード寸法 (12dp/16dp) 記述を Theme 4属性駆動へ、「Section H/F と Cell を角丸背景でまとめる」を「Cell 行のみ・H/F は箱の外」へ追随。
- distilled: add-maui-modern-style (maui/ADR-0023・0024 accepted [実装結果の帰結を追記] / maui-facade.md・native-bridge.md・list-appearance.md 更新 / lessons: impl pain 捕捉 1 件・process success 捕捉 1 件 / deviation なし)
- updated: `maui/api/maui-facade.md` — 公開 API へ `ListStyle` (`SettingsViewStyle { Classic, Modern }`、非 nullable・既定 Classic、Theme 独立経路) と Section 装飾4属性 (`SectionMargin` の論理方向解釈・null = platform 既定委譲・facade 無検証の完全素通し・Classic は上下のみ) を追記。timestamp 2026-08-20。
- updated: `maui/api/native-bridge.md` — Store 外の `setStyle` (序数輸送・定義域外 Classic 正規化・Host 外保持と再生成をまたぐ維持)、`KsBridgeTheme` の Section 装飾7フィールド (フラット論理4成分・all-or-none)、Android の非検証 `PaddingValues` 実装 (`KsBridgeSectionMargin`) による素通し輸送を追記。timestamp 2026-08-20。
- updated: `core/styling/list-appearance.md` — 描画時正規化の対象を「負の寸法成分」から「負および非有限 (NaN・±∞) の寸法成分」へ拡張 (両 OS)。timestamp 2026-08-20。
- distilled: unify-modern-default-corner-radius (core/ADR-0024 accepted [探索時に起票し実装前にユーザー承認済み] / list-appearance.md 更新 / lessons: 新規捕捉なし / deviation なし)
- updated: `core/styling/list-appearance.md` — Modern の既定角丸を「platform 所有・揃えない」から「両 platform で同じ生値 26 に統一 (core/ADR-0024)」へ改訂 (未指定 Theme のクロスプラットフォーム表示を揃えるため)。既定 margin の platform 所有・不統一は維持。timestamp 2026-08-20。

## 2026-08-21

- distilled: upgrade-android-build-toolchain (ADR 新規なし — exploration の判定どおりツールチェーン版の選定と catalog 化は可逆で境界を越えない / `android/architecture/build-toolchain.md` 新設・repository-boundaries.md / test-execution.md / native-bridge.md / public-identifiers.md 追随 / lessons: process pain 捕捉 1 件 (count 2)・spec-review pain 捕捉 1 件 / deviation 4 件)
- created: `android/architecture/build-toolchain.md` — Gradle を動かす JDK (17〜25 実測) と成果物ターゲット (Java 17、`jvmToolchain(17)` はローカル JDK 17 を要求) の役割分担、`android/gradle/libs.versions.toml` をバージョン宣言の単一元とし samples は `versionCatalogs` で共有すること、MAUI binding の Exec 対象 (3 module) と aar 再生成入力、ツールチェーン更新時の互換制約 3 方向と完了条件。android ドメインで architecture/ カテゴリを初使用 (rules.md の注記を更新)。
- updated: `cross/architecture/repository-boundaries.md` — Sample の consumer 境界に「バージョン宣言だけは catalog を両 build root で共有する」を追記 (timestamp 据え置き: MAUI 行など他節は未検証)。
- updated: `cross/conventions/test-execution.md` — Android の実測件数を 2026-08-21 の 1261 × 2 に更新し、Gradle を動かす JDK の選び方と JDK 17 のローカル要件を追記。
- updated: `maui/api/native-bridge.md` — binding 構成の要点に Exec 対象 3 module と aar 再生成判定入力 (catalog / wrapper / gradle.properties) を追記。
- updated: `cross/conventions/public-identifiers.md` — Android を 4 module (bridge 含む) に更新し、GAV の version が catalog の `ks-settingsview` キーを単一宣言元とすることを追記。
- drift: ディープ (`cross/architecture/repository-boundaries.md` 単体、オーナー指定)。検証済み 1 件 / 乖離 4 件 (すべて doc 側の陳腐化: MAUI 行「空の入口」→ facade + binding + tests の入口・Bridge target / module の注記・MAUI Sample の consumer 境界・ルート `global.json` を許容例外として明記。対応: 本文更新、timestamp 2026-08-21)。コード側の問題なし。

## 2026-08-22

- distilled: fix-compose-dsl-double-update-flaky-test (ADR 新規なし — exploration の ADR 候補「なし」を維持。切り分けの結果が実装のタイミング穴ではなくテスト側の待機不備だったため再判定は不要 / `cross/conventions/test-execution.md` 追随 / lessons: test pain 捕捉 2 件 (いずれも count 2)・process success 捕捉 1 件 / deviation なし)
- updated: `cross/conventions/test-execution.md` — Android 節に「非同期反映を待たないアサーション」を新設。`AsyncListDiffer` の差分計算がバックグラウンドスレッドで走り結果を main へ post するため `idle()` / `waitForIdle()` を重ねても待てないこと、待機は deadline での区切り・バックグラウンドへの実行機会の譲渡・タイムアウト時の `fail()` の3点を欠くと「待ったつもり」になること、この失敗が CPU 競合時のみ起きるため全モジュール並列実行でだけ落ちる flaky として表面化することを記載。timestamp 2026-08-22。

- distilled: fix-maui-icon-lease-disposal-ordering (コード変更ゼロ — 起票内容 (review-002 Minor-10 の 3 経路) は指摘元 change 内で修正済み、順序を固定する回帰テストも `IconSourceTests` に 4 本実在し、ミューテーション実測で検出力を確認したため「対応不要」で確定 / ADR 新規なし・maui/ADR-0015 の Consequences に (1) 解消済みの追記 / `maui/architecture/view-materialization.md` 追随 / lessons: process pain 捕捉 1 件 (count 1) / deviation なし。保留 (b) は investigate-maui-icon-lease-sharing へ分離)
- updated: `maui/architecture/view-materialization.md` — 「退役順序」に icon の `KsImageLease` が wrapper と同じ退役キュー・同じ順序規律に従うこと (破棄が走らせるのは画像本体ではなく画像解決サービスの後片付けであること) を追記。「用途をまたぐ再利用の規律」に、退役順序のテストは破棄の有無ではなく破棄の瞬間の配信状況を観測すること、icon 側は後片付けが経路依存 (file / resource 経路は no-op) のため順序を誤っても大半のテストが無症状で通ることを追記。timestamp 2026-08-22。

- distilled: fix-ios-entrycell-writeback-race (コード変更ゼロ — android/ADR-0014 と同型の書き戻しレースが iOS にもあるという前提が実装ゲートの実測で反証されたため「対応不要」で確定。Simulator 3 条件 + 実機の計 8 セット・有効 165 試行で FAIL 0 / ADR 昇格なし: ios/ADR-0004 は起票取り下げで欠番化しドラフトを change 配下へ退避、index に理由注記 / `core/cells/input-cells.md` と `ios/api/ios-native-host.md` 追随 / lessons: spec-review pain 捕捉 1 件 (count 1)・process pain 捕捉 1 件 (count 1)・既存 process パターン reachable-fix-deferred-to-another-change に 1 件追記し count 4 で `process.md` L-005 へ昇格 (オーナー承認) / deviation なし)
- updated: `core/cells/input-cells.md` — 「iOS の同型契約は未検証・未導入」を実測結果へ差し替え。iOS の `EntryCell` はフォーカス中 SSoT 契約を持たず、Host が描画時点で最新 Cell 値を引くため書き戻しエコーで入力中の値が巻き戻らないこと、callback で `text` を更新しない構成でも表示が戻らないこと (Android との非対称) を記載。timestamp 2026-08-22。
- updated: `ios/api/ios-native-host.md` — 「model と表示の同期」に、内容更新が行の描画時点の最新 Cell 値で反映される保証を追記。Controller が更新要求時点で model の索引を同期更新し描画時にそこから引くため、Native への適用が遅延しても古い値が後から画面に現れないこと、入力 Cell の書き戻し往復が値を巻き戻さないのはこの性質によることを記載。timestamp 2026-08-22。

## 2026-08-24

- distilled: fix-cell-icon-size-parity (ADR 新規なし — core/ADR-0025・0026 は探索時に accepted 済みでコードアンカーも両 OS に存在 / `core/styling/cell-row-layout.md`・`core/cells/basic-cells.md`・`core/styling/style-resolution.md`・`cross/conventions/comment-policy.md` 更新 / lessons: 発生時捕捉済み 3 パターン (adjacent-issue / lint-zero / layout-allocation) を確認、蒸留での追加捕捉なし / deviation あり (ButtonCell.titleAlignment の受け入れ + 付随修正 5 件 + comment-policy lint の検出機構削除のオーナー判断。lint 削除判断の ADR 化はオーナー判断で見送り — inbox パターンと comment-policy.md の lint 節更新で担保))
- distilled: fix-ios-root-accessory-theme-refresh (S 級・ADR なし / `core/architecture/display-state-synchronization.md`・`core/styling/style-resolution.md` 更新 — Theme 更新の適用範囲を「text 形式 H/F は追従・View 形式は状態保持のため非追従・Cell は再評価」の三分割へ / lessons: 探索前提の訂正 2 件は stub-filed-issue-not-verified-against-code (count 2) に発生時捕捉済み / deviation あり (探索前提の訂正 2 件 + Section H/F へのスコープ拡張 + Android setRootDirect の付随修正))
- distilled: harden-compose-settingsroot-dsl (M 級・ADR なし — 探索時に 2 候補とも不成立確定 / `android/api/android-compose.md` 更新 — builder `section(...)` が core `Section` と同じ引数・既定値を持つ契約を追記 / lessons: L-005 の出典として発生時反映済み / deviation あり (.gitignore・flaky 修正・Thread.yield 置換の同梱 3 件)。残作業の pump() 条件ベース化は fix-customcell-test-pump-condition-wait として簡易起票)
- updated: `core/styling/cell-row-layout.md` — 主行の幅配分を core/ADR-0026 の新契約 (title コンテンツ幅・valueText 残り幅、狭幅時は valueText が譲る) へ反転。「icon 枠」節を新設し正方形枠 + aspect fit + 枠への角丸 (core/ADR-0025) を明記。titleAlignment が行内 trailing のある行で視覚に出ないことを明確化。timestamp 2026-08-24。
- updated: `core/cells/basic-cells.md` — `ButtonCell.titleAlignment` の効く範囲を「valueText を持たない行に限る」へ明確化 (icon / hintText は幅配分に参加しない)。timestamp 2026-08-24。
- updated: `core/styling/style-resolution.md` — icon size / radius の無効値規則 (size は正の有限値のみ・radius は 0 以上の有限値のみ) と両 platform での枠反映を記載し、「Android は描画へ適用していない」の注記を削除。Theme 更新節を text / View 三分割へ追随。timestamp 2026-08-24。
- updated: `core/architecture/display-state-synchronization.md` — 「Theme 更新の適用範囲 (text / View / Cell)」節を新設し、同期経路表の Theme 行と保証事項を追随。full 更新の共通出口が Theme 通知を欠くと経路依存の非対称が生じることを記載。timestamp 2026-08-24。
- updated: `cross/conventions/comment-policy.md` — 禁止する参照に「変更アーティファクト内のタスク通番」を追加 (lint 実装済み・規約本文が未追随だった)。機械検査節から削除済み `--advisory` の記述を除去し、「lint の検出範囲は規約より狭い / 検出 0 件は適合の証明にならない」を明記。timestamp 2026-08-24。
- updated: `android/api/android-compose.md` — builder `section(...)` の引数対称性 (core `Section` と同一の引数・既定値、文字列ヘッダ版の `footer`) を追記。timestamp 2026-08-24。

## 2026-08-25

- distilled: investigate-maui-icon-lease-sharing (maui/ADR-0026 起票・accepted — iOS の icon 後片付けは解決時の所有権分類で守る (共有 UIImage を破棄しない)。`KsFileImageOwnership.cs` に参照コメント追加 / `maui/api/maui-facade.md` 更新 / lessons: 発生時捕捉済み 1 件 (counterpart-finding-downgraded) を確認、蒸留での追加捕捉なし / deviation あり (参照カウント registry → 所有権分類への設計変更・File.Exists 短絡撤去・代表 1 件保持の廃止・DisposeRetiredViews 付随修正))
- distilled: android-hinttext-position (S 級・ADR なし — exploration どおり局所レイアウト修正 / `core/styling/cell-row-layout.md` は review-002 が乖離なしを検証済みのため timestamp のみ更新 / lessons: 蒸留で pain 2 件を新規捕捉 (提出証跡画像の目視確認漏れ / 撮影規律の反復) / deviation あり (ButtonCell 余白の付随修正 + 隣接修正 2 件))
- distilled: entrycell-keyboard-avoidance-check (S 級・ADR なし — デモ構成の小変更 / `core/cells/input-cells.md` 更新 / lessons: 発生時捕捉済み 1 件 (visual-verification-limited-to-primary-platform) を確認 / deviation なし)
- updated: `maui/api/maui-facade.md` — IconSource の解決に「iOS のキャッシュ所有画像は facade が破棄しない (所有権分類、maui/ADR-0026)」を追記。決定の経緯一覧に maui/ADR-0026 を追加。timestamp 2026-08-25。
- updated: `core/cells/input-cells.md` — 保証することに「キーボード回避はライブラリ実装なしで platform 標準機構により成立 (4 環境実測 2026-08-24、MAUI Android は WindowSoftInputMode 未指定のまま動作)」を追記。timestamp 2026-08-25。
- updated: `core/styling/cell-row-layout.md` — android-hinttext-position の review-002 で乖離なしを検証 (行余白の実現手段は規定せず、hintText の外縁基準配置と整合)。timestamp のみ 2026-08-25 へ更新。

## 2026-08-26

- distilled: retarget-docs-refresh-to-skills (M 級・新規 ADR なし — 決定は cross/ADR-0022 (proposed) の系列。同 ADR の Decision に責務分界「skills/ 一式と manifest の初期生成・Skill 構成の見直しは変更フローの承認を通し、docs-refresh は追従専用・manifest 不在時はフォールバックせず停止」を追記 / concepts 影響なし (docs-refresh は開発ハーネス側の道具で、規約の正は AGENTS.md と kasane/config.yaml。本 change で両方更新済み) / lessons: 発生時捕捉済み 1 件 (sibling-platform-spec-clause-contradicts-shared-contract count+1) を確認、蒸留で pain 2 件を新規捕捉 (自己レビュー周回を spec 妥当性の根拠にしない / モードフラグ修正の全ステップ掃き出し) / deviation あり (--readme-only × コード正チェックの交差を「報告のみ」で確定))
- created: `cross/conventions/aiforms-spec-summary.md` — `docs/legacy-aiforms-reference.md` を concepts へ移送 (rollout-user-skills タスク 1.1)。移植元 AiForms の公開 API・構造・実装パターンの要約。`type: reference` とし冒頭に凍結注記 (以後更新せず、最終的な正は移植元コード) を明記。移植元へのローカル絶対パスと `file://` リンクを `../<リポジトリ名>/` 形式へ変換し、`aiforms-origin-reference.md` と相互リンク (同ファイルの補助資料リンクを新パスへ差し替え)。timestamp 2026-08-26。
- updated: `cross/conventions/comment-policy.md` / `cross/conventions/test-execution.md` — 旧 docs ディレクトリ廃止に伴う前提記述の差し替え (rollout-user-skills タスク 6.1)。comment-policy の適用範囲「対象外」から `docs/` を外し `skills/` を置いた (利用者向け Agent Skills のコード例は「原則コメントを書かず、やむを得ない最小限のコメントは英語で統一する」別規約に従う旨を明記)。test-execution の「README・docs の `swift test` との関係」節を「README の `swift test` との関係」へ改め、案内元を `README.md` に限定し `skills/` がテスト実行手順を持たないことを追記。規約の内容自体は変更なし。

## 2026-08-28

- distilled: rollout-user-skills (M 級 — cross/ADR-0022 を実装後視点で更新し accepted へ昇格 (検収確定の閉世界性・冒頭概念説明・配布座標前提を Decision に反映)、cross/ADR-0014 を superseded 化 / concepts 本文の追加変更なし (aiforms-spec-summary 移送と conventions 差し替えは change 内で実施済み) / lessons: 発生時捕捉済み 2 件 (skill-review-not-simulating-deployed-context / usage-guide-replaces-platform-template-default) を確認、蒸留での追加捕捉なし / deviation あり (閉世界性・概念説明・仮座標・SectionMargin TypeConverter 同梱ほか) / roadmap: package-distribution/phase-12 を completed 化)
- distilled: add-entrycell-placeholder-color (M 級・新規 ADR なし — 既存規約 (標準4段解決・platform default 方針) の踏襲 / `core/cells/input-cells.md` に placeholderColor の解決順と MAUI facade 対応を追記、`core/styling/style-resolution.md` に EntryCell 入力文字色の valueText 準拠を追記 / lessons: 追加捕捉なし / deviation あり (付随修正 2 件 + valueText 最終段是正の波及記録))
- distilled: relax-android-host-prerequisites (L 級 — android/ADR-0021 起票・accepted (カレンダー選択面の回転復元は View インスタンス状態で自前化)。ADR-0018/0019/0020 は探索時に起票済み / `core/styling/style-resolution.md` の platform theme 前提を同梱テーマ隔離へ書き換え、`core/cells/date-picker-selection-surface.md` を Compose カレンダーダイアログ・状態操作ジャンプ・回転復元契約へ大規模改訂、`core/cells/time-picker-selection-surface.md` を新規作成 (時分ホイールシート・12/24h 判定)、`android/api/android-native-host.md` の前提 2 節を「前提なし」へ書き換え + 回転復元節を刷新、`android/api/android-compose.md` の Material3 前提記述を隔離契約へ差し替え、`android/architecture/build-toolchain.md` に Compose 版整合 (BOM ⇔ Xamarin.AndroidX.Compose.*) の規律を追加、`maui/api/maui-facade.md` の FragmentActivity 付随記述を解消 / lessons: 蒸留で pain 1 件を新規捕捉 (worker-used-git-stash-despite-tree-constraint) / deviation あり (33 行 — DayNight ダーク決定・隔離範囲の拡大・Compose 版整合ほか) / Sample 12h 片側先行の追随は changes/align-timepicker-hour-cycle-across-platforms (簡易起票) が受け皿)
- distilled: align-timepicker-hour-cycle-across-platforms (M 級・新規 ADR なし — 中核判断は探索時起票の core/ADR-0028 (accepted) / `core/cells/input-cells.md` の TimePickerCell 契約と `core/cells/time-picker-selection-surface.md` の時制節を is24Hour 一本化 + 系列順の Locale 由来化 (deviation) へ書き換え、`maui/api/maui-facade.md` に Is24Hour を追記 / lessons: 蒸留で境界の観測 1 件を追記 (change-grade — M 維持裁定、count 不変) / deviation あり (1 件 — 12時間制の系列順を Locale 由来へ))
- distilled: restore-pickercell-object-items (L 級・新規 ADR なし — 中核判断は探索時起票の core/ADR-0029 (accepted)、design Decision 1〜8 は同 ADR の枠内 / `core/cells/input-cells.md` に PickerItem モデル・ジェネリック縁・object 書き戻しの節を新設、`core/cells/picker-selection-surface.md` に副表示 (subText・description 系統継承・可変行高) を追記、`maui/api/maui-facade.md` の PickerCell 節を object API (射影・相互導出・保留復元・正規化境界) へ全面改訂 + TwoWay 12 プロパティ化、`maui/api/native-bridge.md` に per-item 候補 DTO を追記 / lessons: 発生時捕捉済み 1 件 (cross-platform-text-compared-without-locale-control) を確認、蒸留で success 1 件を既存パターンへ追加捕捉 (review-recommended-fix-treated-as-hypothesis count 4→5、閾値到達 → オーナー承認で impl L-005 へ昇格) / deviation あり (裁定 2 件 + 実装範囲注記 + 付随修正 5 件))
- created: `maui/conventions/performance-verification.md` — 描画性能の評価は必ず Release ビルドで行う規約 (change: customcell-android-maui-perf)。MAUI Android の Debug は Mono インタープリタ実行のため実力より大幅に遅く見え (Pixel 6a 実測 2026-08-28: Debug Janky 31.7% / p90 121ms、Release 4.6% / 12ms、native サンプル 6.1% / 28ms)、Debug の遅さは単独では実装欠陥の証拠にならないこと (実装を疑うかは Release の計測で判断)、`UseInterpreter=false` は中間 (8.8〜19.4% / 53〜65ms) で Release の代替にならないこと、計測手順と証跡の所在、iOS 側は未計測 (「iOS では問題が出ない」は Simulator 経路の観察で非同一土俵、実機 Debug の乖離は推定にとどまる) と留保付きで「Android だけ遅い」報告の第一容疑者がビルド構成であることを明記。`maui/index.md` に conventions/ 節を新設して 1 行追加、`rules.md` の platform カテゴリ表に conventions/ 行を追加 (2026-08-28 オーナー合意)。timestamp 2026-08-28。
- distilled: customcell-android-maui-perf (S 級 ksn-live・新規 ADR なし — 「性能確認は Release で」は上記 created の policy で十分と判定 (覆すコスト低)、rules.md の新カテゴリ条件の明文整理はオーナー判断で見送り (現状維持、次の同種議論で再判断) / concepts は上記 created 行のとおり実装中に作成済み、蒸留での追加変更は evidence 参照の archive 後パス確定のみ / lessons: pain 3 件 — device-evidence-left-in-session-scratchpad へ追加捕捉 (count 1→2)、unmeasured-inference-hardened-in-longlived-doc 新規、developer-facing-knowledge-added-to-user-readme 新規。昇格なし (いずれも閾値未達) / deviation なし (デルタスペックを持たないライブ調整) / 見送った構造課題 2 件は changes/maui-android-customcell-embed-perf (簡易起票) が受け皿)
- created: `cross/conventions/user-skill-api-listing.md` — 利用者向け Skill (skills/) への公開 API 掲載基準 (change: skills-api-coverage)。「簡潔でも網羅 (プロパティ名・機能名が一度は見つかること)」を方針とし、意図的な掲載除外の4基準 (低頻度の細部 / 内部層・interop 層 / 機械的に導出できる名前 / 可視性引き下げ候補) と platform 別の現行除外リストを表で持つ。3e (docs-refresh の API 名網羅検査) はこの表を参照しないため、表の役割は報告の抑止ではなく仕分けの高速化であることを明記。`cross/index.md` の conventions 節に 1 行追加 (2026-08-29 オーナー合意、初見可読性レビュー実施済み)。timestamp 2026-08-29。
- distilled: skills-api-coverage (S 級・新規 ADR なし — 掲載基準は「決定の why」より「更新し続ける決まり事」の性質が強く、ADR ではなく上記 created の policy concept として長命化 (2026-08-29 オーナー判断) / concepts: `ios/api/ios-native-host.md` に Theme default 定数 18 件の表を追記し store-less init の記述を実装 (internal init) に合わせて是正、`android/api/android-native-host.md` に `DEFAULT_*` 定数 14 件の表を追記、`maui/api/maui-facade.md` に「スタイルプロパティ一覧」節 (SettingsView 42 / CellBase 20) と `EntryCell.TextAlignment` を追記 / lessons: pain 1 件を新規捕捉 (api-enumeration-written-as-inline-list — 5 件超の API 列挙は表にする)。昇格なし (閾値未達) / deviation なし (デルタスペックを持たない S 級) / 派生: docs-refresh スキルに 3e (API 名網羅検査) と内容規約⑦⑧ (個数ハードコード禁止・全称表現禁止) を新設、skills 24 ファイル (3 Skill × en/ja) を追従更新 / 残課題は changes/docs-refresh-3e-hardening と changes/ios-effectivestyle-visibility (いずれも簡易起票) が受け皿)

## 2026-08-29

- created: `maui/architecture/binding-build-integration.md` — MAUI binding の Native artifact 統合契約 (change: consolidate-readmes-and-contribution)。iOS の標準 `XcodeProject` アイテム + `CreateNativeReference=false` + 手動 `NativeReference` の構成と増分入力の補正、Android の `gradlew` Exec による aar 生成、`BG8605` / `BG8A00` 警告の意味と抑止しない理由、生成型の `KsSettingsView.Bridge` namespace 統一 (Android は `Transforms/Metadata.xml` の `managedName` で上書き)、SDK 更新時に再検証する SDK 内部 target の表 (maui/ADR-0006 が言う「再検証の入口」)。移送元は廃止した `maui/README.md`。`maui/index.md` に 1 行追加。初見可読性レビュー実施済み (独立文脈、Major 4 件を反映)。timestamp 2026-08-29。
- created: `maui/conventions/integration-host-verification.md` — MAUI 検証ホスト (IntegrationHost / MauiHost) の実行規約 (change: consolidate-readmes-and-contribution)。iOS / Android の起動手順 (`DEVELOPER_DIR` の指定を含む)、期待される表示、解放 → 再生成シナリオの確認手順。C# から native 表示までの end-to-end 疎通を確認する回帰資産としての位置づけ。移送元は廃止した `maui/README.md`。`maui/index.md` に 1 行追加。初見可読性レビュー実施済み (独立文脈、Major 4 件を反映)。timestamp 2026-08-29。
- created: `cross/conventions/local-development-setup.md` — clone から Sample 実行・本体ビルド・ステップインまでのローカル開発手順 (change: consolidate-readmes-and-contribution)。`ANDROID_HOME` と 2 つの `local.properties`、`DEVELOPER_DIR` による Xcode 選択、3 platform の Sample 起動、本体モジュールのビルド / lint コマンド (旧ルート README と旧 `android/README.md` から git 復元)、本体 source へのステップイン (MAUI は `ProjectReference` を根拠に新規記述)。デモ画面一覧は転記せず各 `SampleScreen` の実ソースを正と規定。移送元は廃止した `android/README.md` / `samples/*/README.md`。`cross/index.md` に 1 行追加。初見可読性レビュー実施済み (独立文脈、Major 4 件を反映)。timestamp 2026-08-29。
- updated: consolidate-readmes-and-contribution — 上記 3 本の新設に伴う追随と、README 廃止による参照整合。`maui/api/native-bridge.md` の「正は `maui/README.md`」参照 2 箇所を binding-build-integration.md へ解消 / `cross/conventions/runtime-behavior-verification.md` に iOS 基本 Cell Sample の目視確認項目を統合 / `core/styling/style-resolution.md` にライブラリ既定色を中立に保つ方針を追記 / `cross/conventions/public-identifiers.md` から未公開を理由とした記述制限 2 項目を削除 (+ 欠落していた H1 を追加) / `maui/conventions/performance-verification.md` の `samples/maui/README.md` 参照 2 箇所を差し替え / `cross/conventions/test-execution.md` に MAUI 節を新設 (2026-08-29 実測 516 件 / 0 失敗) し `swift test` 節から廃止済み README への前提を除去。timestamp はいずれも 2026-08-29。
- distilled: consolidate-readmes-and-contribution (L 級 — cross/ADR-0023 (README はルート 2 枚、開発者向け知識は concepts へ一本化) と cross/ADR-0024 (貢献は Issue で受け外部 PR は受けない) を accepted へ昇格し、実装で判明した帰結を Consequences に追記 (0023: 「他所に既出」を根拠にした破棄が本 ADR 自身の適用で循環していた件、`maui/ADR-0006` の参照切れと supersede しない判断 / 0024: `contact_links` 未設定で質問窓口が無い件)。新規 ADR の起票なし — design.md の 3 Decision はいずれも ADR-0023 / 0024 の適用範囲の具体化で選別 3 基準に該当せず / concepts は上記 created 3 本 + updated のとおり実装中に確定済み、蒸留での追加変更なし / lessons: 実装中に 4 件を捕捉 (transfer-table-enumerated-by-source-not-content-class [spec-review] / counterpart-sandbox-blocks-file-operations [process, count 2] / counterpart-quota-consumed-by-impl-starves-gates [process] / batched-edit-discarded-on-later-assertion [impl])。いずれも閾値未達で昇格なし / deviation あり (12 項目) / roadmap: package-distribution/phase-9-docs を completed 化)

## 2026-08-30

- distilled: add-question-form-and-english-screenshots (M 級 — concepts の更新なし。3 件の決定は実装前に長命層へ反映済みで、cross/ADR-0024 が Issue テンプレート 3 本化、cross/ADR-0023 が `maui/spike/` の非公開と公開リポジトリの README 4 枚を既に記録している。repository-docs / docs-refresh に対応する概念ファイルは存在せず、スクリーンショットの撮影条件と対訳表は phase-2 の決定が「change の `ui/brief.md` に記録して再現可能にする」と定めたとおり archive 側に残す。実装乖離なし。教訓 1 件を lessons/process.md L-006 へ昇格)
- migrated: `concepts/*/conventions/` の規範型 10 件を `kasane/handbook/` へ移送 (ksn-concept モード3。判定基準は ksn-core references/handbook.md の「文書とコードが食い違ったとき直すのはどちらか」)。cross 8 件 (comment-policy / test-execution / runtime-behavior-verification / sample-parity / local-development-setup / user-skill-api-listing / aiforms-origin-reference / public-identifiers)、maui 2 件 (integration-host-verification / performance-verification)。frontmatter を `kind` (rule 9 / guide 1 — local-development-setup) + `applies-when` へ付け替え、`type` と `tags` は削除。**timestamp は据え置き** (移送は内容の検証ではない)。`handbook/index.md` (ドメイン地図) と `handbook/{cross,maui}/index.md` (適用のきっかけ列つき一覧) を新設。public-identifiers.md は規範 (命名方針) と記述 (「Maven 座標の現在地」= ADR-0002 と現行 Gradle `group` の drift) が同居するが、主題が命名規範のため分割せず丸ごと handbook へ (2026-08-30 オーナー判断)。user-skill-api-listing.md には欠落していた h1 を追加。
- moved: `cross/conventions/aiforms-spec-summary.md` → `cross/reference/aiforms-spec-summary.md` — 移植元コードという外部の実物との照合で真偽が決まるため concepts に残置。カテゴリ `cross/reference/` を新設 (2026-08-30 オーナー合意)。
- updated: `rules.md` に「規範は handbook へ」節を追加し、cross カテゴリ表の `conventions/` を `reference/` へ差し替え、platform カテゴリ表の `conventions/` 行を削除 (timestamp 2026-08-30)。`concepts/index.md`・`cross/index.md`・`maui/index.md` に handbook への誘導を追加。参照の張り直しは生きている文書のみ (2026-08-30 オーナー判断) — concepts 7 / decisions 4 / lessons 7 / 進行中 change 2 / roadmap agenda 7 + exploration / `config.yaml` / comment-policy 系スクリプト 3 本と hook / docs-refresh SKILL.md / `samples/android/.../SampleTheme.kt` のコメント 2 箇所。`changes/archive/`・各 `history.md`・本 log の過去エントリは当時の記録として据え置き。ADR は cross/0016・cross/0017・cross/0023・android/0016 のリンク文字列のみ更新 (決定内容は不変)。cross/0016・cross/0017・phase-12 agenda は移送前から href の深さが 1 段ずれて壊れていたため併せて修正。
- 残課題 (移送では手を付けない): `skills/.manifest.json` は docs-refresh の管轄のため未更新 — 次回実行で旧 `cross/conventions/*` 9 件が DELETED、`cross/reference/aiforms-spec-summary.md` が新規として検出される。移送した comment-policy / sample-parity / test-execution には移送前からの構造 lint 違反が計 14 件残る (concepts 全体の既存債務 357 件の一部)。

## 2026-08-31

- distilled: add-verification-ci (M 級 — **concepts の更新なし**。検証 CI の構成は workflow 定義から再導出でき、変更のたびに追随を要するため concepts に置かない。腐りにくい「なぜその構成か」「何を保証し何を保証しないか」は ADR 2 件が持つ。既存の `cross/architecture/repository-boundaries.md` は monorepo と build root の責務分担が主題で、CI は別主題のため追記もしない)
- decided: [cross/ADR-0025](../decisions/cross/0025-verification-ci-reusable-platform-workflows.md) (検証 CI は platform 別の再利用可能 workflow と、それを呼ぶ入口で構成する) と [cross/ADR-0026](../decisions/cross/0026-ci-guarantee-logic-and-wiring-not-e2e.md) (CI が保証するのはロジックの全件通過と native への配線のコンパイルまでとし、実機・実行ホストでの検証は手元の手順に残す) を accepted で起票。フェーズ議論の決定 10 件のうち、後続の設計を制約する 2 件のみを昇格し、残る 8 件 (トリガー・Xcode / JDK の版選択・キャッシュ構成・ランナー選択ほか) は workflow を書き換えれば済む可逆で局所的な判断として ADR にしていない。
- updated: `handbook/cross/test-execution.md` — 収束を待つアサーションの 3 条件 (実時間 deadline / 実行機会を譲る / 超過時に実測値付きで fail) を Android 節から **platform 共通の節へ引き上げ**、iOS での現れ方 (RunLoop を固定秒数まわす待機) を追加 (timestamp 2026-08-31)。同じ誤りが 3 変更で platform をまたいで再発したため (教訓 `fixed-time-wait-instead-of-condition-based` count 3 の昇格先を lessons ではなく規範層とした — 規範は既に存在し、問題は適用範囲が Android に閉じていたこと)。
- promoted: `lessons/process.md` L-007 (レビュー・セカンドオピニオンへ渡す成果物が実態を反映しているかを起動前に確認する。既知の切り出し済み先送り問題に加え、実施済みだが未反映の作業も対象)。教訓 1 件を新規捕捉 (`spec-requirement-targets-nonexistent-external-resource`、count 1 で inbox 保留)。
- 残課題: `handbook/cross/test-execution.md` の構造 lint 違反 5 件は本改訂の前から Android 節に存在し、件数は 5 → 5 で増減なし (今回は解消しない)。

## 2026-09-01

- distilled: identity-lint-adb-colon-pattern-gap (S 級スタブ — 蒸留物なし。identity-lint の `adb:` ラベル形式検出と `git grep --untracked` は Kasane 本体で修正済み・本リポジトリの scripts/ へ再配置済みで、検出形式の知識は ksn-core references/evidence.md 側が持つ。ADR 候補なし (可逆な lint パターン追加)。lessons: `lint-zero-treated-as-convention-compliance` への合流を検討したが、発見元 entrycell-keyboard-avoidance-check の review はレビューが手動検出に成功しルート修正まで完了した事例のため pain として数えない。deviation なし)
- distilled: fix-ios-tapnotifyingrenderer-actor-isolation (M 級・新規 ADR なし — 解消手段の判断は internal 1 行で可逆と探索時に判定済み / handbook: `ios/swift6-language-mode-check.md` を新設 (proposal What Changes (4) の申し送り — iOS source を触る変更の完了判定前に Swift 6 言語モード一時設定ビルドでエラーゼロを確認する rule)。`handbook/ios/index.md` 新設と `handbook/index.md` の地図更新も実施 / concepts: 変更なし (internal のみ・挙動不変で、既存概念に Swift concurrency / deinit の記述なし) / lessons: 発生時捕捉済み 3 件 (ask-omits-relation / question-answered-with-question-dialog count 2 / test-limitation-asserted count 2) を確認、蒸留で shared-setup-wait-predicate-narrower-than-callers-read へ独立 2 件目として追加捕捉 (count 1→2 — 並行ブランチで狭い private 待機ヘルパを新設していた) / deviation あり (1 件 — 保守注意書きの実在箇所が tasks の想定と相違) / 媒体 6 件削除)
- distilled: fix-ios-test-pump-condition-wait (M 級・新規 ADR なし — 中核判断は探索時起票の cross/ADR-0027 (accepted)。handbook `cross/test-execution.md` の負の検証例外・iOS 待機ヘルパ表・Root accessory 追従の実挙動反映は実装スコープで完了済み (timestamp 2026-09-01) / concepts: 変更なし (待機ヘルパはテスト支援でコードから再導出可、規範は handbook 側が持つ) / lessons: 発生時捕捉済み 5 件 (adjacent-issue-deferred count 2 / stale-worktree / counterpart-sandbox count 3 / shared-setup-wait-predicate / verdict-buried count 2 / warning-count-taken) を確認。counterpart-sandbox-blocks-file-operations が pain 閾値 3 到達 → オーナー承認で process L-008 へ昇格 (詳細は details/ へ移送、inbox 削除) / deviation あり (分類台帳との差分 3 件・置換粒度の非対応・オーナー判断のスコープ追加 2 件))
- distilled: fix-customcell-test-pump-condition-wait (S 級スタブ — 蒸留物なし。Android テスト 2 箇所の pump() を既存規約準拠ヘルパ awaitDifferCommit の再利用で条件ベース化する直接実装 (8737c5e)。ADR 候補なし (既存規約 test-execution.md への追従)。concepts / handbook 変更なし。lessons 追加捕捉なし。deviation なし (デルタスペックを持たない S 級))
- distilled: share-wait-helpers-in-ios-cellviewsupport-tests (S 級スタブ — 蒸留物なし。KsCellViewSupportTests の private 待機ヘルパ 2 つを共有ターゲットの awaitInitialRender / awaitCondition へ置き換える直接実装 (04b6a6c)。ADR 候補なし。concepts / handbook 変更なし。教訓は同件を shared-setup-wait-predicate-narrower-than-callers-read の 2 件目 (発生元 fix-ios-tapnotifyingrenderer-actor-isolation) として計上済み。deviation なし)
- distilled: add-android-maven-distribution (L 級・phase-5-android-packaging 完了 — ADR: 新規起票なし (design Decision 1〜6 は設定レベルで可逆、配布形の決定は既存 ADR が保持 — design の申し送りどおり)。[android/ADR-0016](../decisions/android/0016-single-module-single-maven-artifact.md) を proposed → **accepted** へ昇格し、Consequences に実装結果 2 件 (`api` スコープ仕分けの成立条件と Explicit API mode への申し送り / vintage engine によるテスト同居の成立) を追記。cross/ADR-0018 の日付付き追記は change 内で実施済み / handbook: 変更なし (public-identifiers.md は change 内で改訂済み) / concepts: `android/architecture/build-toolchain.md` 更新 (2 module 構成・catalog キー `kssettingsview`・「Maven 発行の入口」節の新設・MAUI binding の aar 2 本化、timestamp 2026-09-01)、旧 module 名の追随 5 件 — `cross/architecture/repository-boundaries.md` (公開単位を単一 artifact へ) / `maui/api/native-bridge.md` / `core/cells/ks-image.md` / `core/core-model/settings-tree.md` / `core/cells/input-cells.md` (層は Kotlin パッケージ表現へ)。ADR の `出典:` 行に残る旧パス (core/0013・0014 等) は当時の実体を指す歴史記述として据え置き (review-001 の推奨どおり) / lessons: `principle-with-enumeration-not-swept-against-public-surface` へ同一 change 内の再発 (foundation-layout、review-001 Major) をカウント外で追記 (count 1 のまま)。閾値超えなし / deviation あり (`api` 列挙の拡張 2 件 + 付随修正 6 分類) / 媒体 2 件削除)
- distilled: add-spm-distribution (L 級・phase-4-ios-packaging 完了 — ADR: 新規起票なし (design Decision 1〜4 はスクリプト局所契約・一度きりの検証手順で選別 3 基準外)。[cross/ADR-0018](../decisions/cross/0018-distribution-public-channels-root-swiftpm-manifest.md) を proposed → **accepted** へ昇格し、Consequences に実装結果 2 件 (staticlib archive への SwiftUI オブジェクト混入の受容 / scheme 一本化) を追記 / handbook: 変更なし (public-identifiers.md・test-execution.md は change 内で改訂済み) / concepts: `cross/architecture/repository-boundaries.md` 更新 (iOS 公開単位を umbrella `KsSettingsView` 1 本へ、配信リポジトリ `KsSettingsView-SPM` の位置づけと「開発の入口にしない」を追記、timestamp 2026-09-01) / lessons: 新規捕捉 1 件 (`destructive-script-spec-lacks-safety-contract` — 相方 spec-review Critical 由来)、`unmeasured-inference-hardened-in-longlived-doc` へ 2 件目を追加 (count 1→2、handbook への未実測手順)。閾値超えなし / deviation あり (実測所見 2 件 + 付随修正 6 件) / 媒体 0 件)
- distilled: adopt-android-explicit-api-mode (M 級 — 新規 ADR なし。中核判断は探索時に accepted 済みの [android/ADR-0022](../decisions/android/0022-explicit-api-strict-for-public-library.md) が保持し、個別 3 API の降格と platform / phase のスコープ判断は局所的な変更境界として選別基準外 / handbook: `cross/user-skill-api-listing.md` の Android 除外リストから internal 化した `SettingsRootStore.preview`・`KsCellRegistry.viewTypeOf`・`isRegistered` を除去 / concepts: 変更なし (公開契約の既存記述に 3 API は含まれず、Strict の why は ADR、設定の現仕様はコードが保持) / lessons: `self-review-iteration-mistaken-for-spec-validity` へ 2 件目を追加捕捉 (count 1→2)、昇格閾値未達 / deviation あり (付随修正 4 件) / 媒体 0 件)

## 2026-09-02

- distilled: add-maui-nuget-distribution (L 級・phase-6-maui-packaging 完了 — ADR: 新規起票なし (design の申し送りどおり。Decision 1〜7 は maui/ADR-0025 の実装方式か既存 ADR の帰結で、選別 3 基準を単独で満たす新規決定はない)。[maui/ADR-0025](../decisions/maui/0025-nuget-three-package-root-namespace.md) を proposed → **accepted** へ昇格し、実装結果 5 件 (SDK 標準 pack と消費者検証の成立 / `buildTransitive/` ガードの同梱 / `SwitchCell`・`EntryCell` の MAUI 本体との型名衝突と受容 / `PackageReadmeFile`・icon 同梱アイテムの置き場 / manifest 絶対パスと自 assembly 用 aar) を Consequences に追記。[maui/ADR-0010](../decisions/maui/0010-androidx-conflict-absorbed-in-binding-layer.md) に NuGet 経路の実証 (未検証項目の解消) と CPM 移行、[maui/ADR-0026](../decisions/maui/0026-ios-icon-cleanup-via-ownership-classification.md) に照合キーが MAUI 10.0.60 以降の内部挙動に依存し要求版 10.0.70 がそれを保証することを日付付き追記 / handbook: 変更なし (`cross/public-identifiers.md` の NuGet 節は change 内で改訂済み) / concepts: `maui/api/maui-facade.md` に「導入と前提」節を新設 (NuGet 座標・xmlns / using の最小例・要件表 — Maui.Controls 10.0.70 / Android 29 / iOS 16.0 とガード `KSSV0001` の現れ方・型名衝突の回避) し「現時点の範囲」の配布記述を更新、`maui/architecture/binding-build-integration.md` に「NuGet パッケージ化」節を新設 (3 パッケージの中身と依存・共通設定 3 ファイルと MSBuild 評価順序・最低 OS 版ガードの契約・受け入れた SDK 挙動 5 件) と pack 成果物の再検証観点を追記、`maui/api/native-bridge.md` の binding 構成に推移参照の位置づけを追記、`cross/architecture/repository-boundaries.md` の MAUI 公開単位を 3 パッケージ (pack 可・未発行) へ更新。timestamp はいずれも 2026-09-02 / lessons: 発生時捕捉済み 3 件 (counterpart-review-catches-evidence-completeness count 2 / public-name-collision-counted-from-single-instance / attribution-finding-without-checking-origin-copyright-holder) を確認。蒸留で spec-review の pain 1 件を新規捕捉 (spec-scenario-verifies-proxy-not-required-property — 相方 spec-review が検出した Scenario の代理観測)、public-name-collision へ同一 change 内の再発 (SDK 生成 aar の deviation が facade 側にしか記録されず) をカウント外で追記。閾値到達なし・昇格なし / deviation あり (12 項目: 置き場 2・SDK 挙動 3・検証手順 1・型名衝突 2・付随修正 2・記録 2) / 媒体 2 件削除)
- 残課題: `maui/api/maui-facade.md` は散文 16101 字で分割検討トリガー (10000 字) を超えている (構造 lint 違反 31 → 30 件、いずれも本改訂前からの債務)。`maui/api/native-bridge.md` も 10684 字・違反 19 件で同様。分割・構造整備は ksn-concept (作庭) の別作業として [changes/restructure-maui-api-concepts](../changes/restructure-maui-api-concepts/exploration.md) に簡易起票 (2026-09-02 オーナー合意)。
- distilled: docs-refresh-worker-dispatch (S 級 — ADR: 新規起票なし (器を `ksn-implementer` に名指し固定し Kasane 非依存を保つ判断は Step 5 の記述を戻せば覆せる可逆な変更境界で、選別 3 基準外。根拠は `.agents/skills/docs-refresh/SKILL.md` の「コンテキスト節約方針」が正 — 器が担うのは編成の切り離しであり、トークン削減の主因は SKILL.md の減量側) / handbook: 変更なし (「共有配置 `.agents/skills/` のスキルを 1 環境の語だけで書かない」はハーネス横断の規律として lessons 経由で育てる) / concepts: 変更なし (docs-refresh の手順は concepts の記述対象外) / lessons: 発生時捕捉 1 件 (shared-skill-written-in-single-runtime-vocabulary — オーナー指摘、count 1)。閾値到達なし・昇格なし / deviation あり (2 件: 予定 manifest の判断入力を JSON 化・SKILL.md 20KB 目標未達 44KB) / 媒体 0 件)
- distilled: add-consumer-verification (L 級・phase-7-consumer-verification 完了 — ADR: 新規起票なし (design の申し送りどおり Decision 1〜6 は `verification/` と CI 定義に閉じた可逆な判断。deviation の `smoke` + artifact 拒否も workflow 入力の契約として同列)。proposed の [cross/ADR-0020](../decisions/cross/0020-release-dispatch-tag-last-version-injection.md) の Consequences に実装結果 2 件 (Gradle 側の `-Pversion=` 受け口を本 change で実装 / 鍵なし発行は未署名で成功) を日付付きで追記 (accepted への昇格は release workflow の蒸留時) / handbook: `cross/local-development-setup.md` に「消費者検証を手元で回す」節と `verification/android` の SDK 解決順を追記 (timestamp 2026-09-02)、`cross/public-identifiers.md` に消費者検証 application ID の導出例を追記、`handbook/cross/index.md` の適用のきっかけを更新 / concepts: `cross/architecture/repository-boundaries.md` に「消費者検証 (`verification/`) の境界」節を新設 (dry-run / smoke の参照先表・排他性・2 段スクリプトと artifact 契約・検証範囲) と保証 / 禁止の追記、`maui/api/maui-facade.md` の要件表に API 版付き TFM の要件 (android 36.0 / ios 26.0 以上、下回ると静かに binding が欠ける) を追加、`maui/architecture/binding-build-integration.md` の API 版付き TFM 行を実測結果へ更新、`android/architecture/build-toolchain.md` の「Maven 発行の入口」に version 注入と署名の条件を追記 (timestamp 2026-09-02)。`cross/index.md` の 1 行説明を更新 / lessons: 発生時捕捉済み 2 件 (adr-decided-mechanism-assumed-implemented-in-tasks / decision-context-crammed-into-choice-dialog) を確認。蒸留で `self-review-iteration-mistaken-for-spec-validity` へ 3 件目 (count 2→3、pain 閾値到達 → 昇格候補)、`spec-scenario-verifies-proxy-not-required-property` へ 2 件目 (「tag 数 0」の代理観測) を追加、新規捕捉 2 件 (new-build-root-added-without-walking-handbook-setup-paths [impl] / incidental-fix-recorded-after-the-fact-in-deviation [process]) / deviation あり (付随修正 3 件 + 契約の補完 1 件) / 媒体 0 件 / `kasane/config.yaml` の `repo.local` 許可コメントを archive 後のパスで由来を辿れる形に改めた (review-002 の申し送り))
- promoted: `lessons/spec-review.md` L-002 (自己レビューの周回数を spec 妥当性の根拠にせず、指摘 0 なら検査軸を変える — Requirement の交差 / 実体との突合 / 責務の閉路。出典: retarget-docs-refresh-to-skills / adopt-android-explicit-api-mode / add-consumer-verification。オーナー承認 2026-09-02。詳細は details/ へ移送、inbox 削除)
