# 機密情報・個人情報の混入点検 (2026-08-21)

ksn-scout による読み取り専用の点検結果。対象は作業ツリーと git 全履歴 (235 commit、全 blob を `git grep` で網羅走査、サンプルではない)。秘密値・個人識別子の値そのものは本ファイルに記載しない。

## 1. 秘密情報 — 所見なし

- 高精度パターン (AWS / GitHub PAT / OpenAI / Slack / PRIVATE KEY ブロック / NuGet apikey / Google API key / GitLab PAT): 作業ツリー・履歴とも 0 件
- 機密系ファイル (`*.jks` `*.keystore` `*.p12` `*.pem` `*.mobileprovision` `local.properties` `google-services.json` `.env` `secrets.*` `xcuserdata` `.DS_Store`): 全履歴の追加ファイル 2656 件中、一度も追加されていない
- 作業ツリーの `android/local.properties` `samples/android/local.properties` `.DS_Store` `*.xcuserstate` `embedded.mobileprovision` はすべて未追跡 (.gitignore 済み)
- 追跡下の `gradle.properties` 3 件は JVM 引数・AndroidX フラグのみ。`.mcp.json` `.codex/config.toml` `.claude/settings.json` にトークン類なし
- 唯一の signing/ossrh 系ヒットは Maven Central FAQ の URL (`kasane/changes/archive/2026-07-18-migrate-openspec/candidates/batch-a-integration.md:90`)、値ではない

## 2. メールアドレス

- 内容中: 架空のデモデータのみ (`*@example.com`、モック HTML 1 箇所の `taro@kamusoft.jp`)
- コミット author/committer: 3 種。うち 1 種が **noreply でない個人アドレス (hotmail)** で、全 235 commit の大半を占める。残りは `*@users.noreply.github.com` と `noreply@github.com`
- public 化すると履歴から閲覧可能。隠すには `git filter-repo --mailmap` 等の履歴書き換えが必要

## 3. ローカル絶対パス — 追跡ファイルに約 1000 行

ディレクトリ別 (行数): `kasane/changes` 462 / `openspec/changes` 67 / `kasane/roadmaps` 9 / `docs` 9 / `openspec/drafts` 7 / `kasane/decisions` 3 / `samples` 2 / `kasane/concepts` 2 / `android` 1

種類別:
- 自リポジトリへの自己参照 (`/Volumes/.../KsSettingsView/...`): 866 — レビュー結果の該当箇所リンク等。機密性なし、ローカル環境構成は露出
- 非公開ローカルクローンへの参照: `AiForms.Maui.SettingsView` 100 / `AiForms.Maui.NativeCollectionView` 22 / `AdMobMediation.Maui` 1
- worktree パス `/Users/.../CCManagerRepos/...`: 12 (履歴には削除済み分がさらに多数)
- codex 連携のレスポンスパス `~/.kasane/counterpart-bridge/responses/...`: 12 — `second-opinion-*.md` 末尾の `KSN_COUNTERPART_META` 行。session_id も併記
- `~/Library/Developer/Xamarin/android-sdk-macosx`: 2、`~/.agents/skills/...`: 1
- `android/README.md:71` / `samples/android/README.md:80,82` は `/Users/<USER>/...` のプレースホルダ (問題なし)

長命層・利用者向け docs の他リポジトリ参照 (公開時に目立つ箇所):
- `kasane/concepts/cross/conventions/aiforms-origin-reference.md:23-24` — AiForms 2 件のローカルパス表
- `kasane/decisions/cross/0018-*.md:63`、`kasane/decisions/cross/0019-*.md:36`、`kasane/decisions/maui/0025-*.md:53` — KsDialogs (未公開リポジトリ) の ADR パスを翻案元として記載
- `kasane/roadmaps/package-distribution/exploration.md:23` — KsDialogs が未公開である旨
- `kasane/roadmaps/maui-support/roadmap.md:29`、同 `phases/phase-2*/history.md:7`、`phase-6*/artifacts/research-*.md:3`、`phase-6*/history.md:5`、`phase-6*/agenda.md:24`
- `docs/legacy-aiforms-reference.md` 9 件 (複数は `file:///Volumes/...` リンク) — 利用者向け docs、修正は docs-refresh 経由

## 4. 個人特定情報

- Apple Team ID: `samples/ios/KsSettingsViewSample.xcodeproj/project.pbxproj:354,385` の `DEVELOPMENT_TEAM` に個人 Apple Developer アカウントの値。履歴では 1 コミットで導入。機密ではないが個人アカウントに紐づく識別子
- Bundle ID: `jp.kamusoft.kssettingsview.*` (組織ドメイン、個人要素なし)
- 氏名: モック HTML の入力欄サンプル値 (`"Satoshi"`) のみ。ホスト名・`.local`: なし
- `*.xcuserstate` / `xcuserdata/` / `.DS_Store` の履歴混入: なし
- 参考: 追跡下に PNG 607 / MOV 10 / MP4 4 (`kasane/changes/**/evidence/`)。シミュレータのスクショと見られるが画面内容は未目視。`.git` は 182MB

## 5. `.gitignore` の健全性

- カバー済み: `.DS_Store` / `local.properties` / `xcuserdata/` / `*.xcuserstate` / `DerivedData/` / `bin/` `obj/` / `build/` `.gradle/` / `.env` / `node_modules/` / `.claude/settings.local.json`
- 欠落: `*.jks` `*.keystore` `*.p12` `*.pfx` `*.pem` `*.mobileprovision` `google-services.json` `secrets.*` `.env.*` `keystore.properties`

## 6. ツール

- `gitleaks` 8.30.1 を導入 (brew) し追加実行: `gitleaks git --redact` (HEAD 履歴 199 commit・17.9 MB) と `--log-opts="--all"` の両方で **0 件**。`gitleaks dir` (未追跡含む作業ツリー) は `DerivedData/` `.gradle/` 等の未追跡ディレクトリでタイムアウトしたため打ち切り (追跡ファイルは git スキャンでカバー済み)
- `trufflehog`: 未導入 (秘密値 0 件のため verified 検出の出番がなく見送り)
- `git-filter-repo`: インストール済み

## 総括

- **履歴書き換えが必要なもの**: 厳密にはなし。方針判断として (1) コミット author の個人メール、(2) 履歴中の削除済みファイルに残るローカルパス (実害小、書き換え非推奨)
- **作業ツリーの修正で済むもの**: docs/legacy-aiforms-reference.md、長命層のローカルパス (KsDialogs 参照 4 箇所はリポジトリ名だけ残しパスを落とす案)、`DEVELOPMENT_TEAM` (空文字化 + xcconfig 外出し案)、`.gitignore` 補強、`second-opinion-*.md` の `KSN_COUNTERPART_META` 行の一括削除
- **放置してよいもの**: デモデータ、README のプレースホルダ、gradle.properties、Bundle ID、`kasane/changes/**` の自己参照パス (866 件。量が多いので公開対象から外す選択肢もある)
- **未検証**: evidence 配下のスクショ・動画の画面内容 (エントロピーベース検出は gitleaks で実施済み・0 件)
