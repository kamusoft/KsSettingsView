- tasks 0.3 (切り分けゲート): spec / design は「MAUI iOS で表示中の外観切替で行背景が白のまま残る」不具合の修正 (Decision 1: iOS 本体の trait 観測主体 / Decision 2: MAUI containment の再解決) を前提 → 実測で症状が再現せず (MauiHost iOS 3 条件 × 2 OS 版・iOS Native 単体・containment 成立)、オーナー裁定により探索へ差し戻し。不具合修正 (tasks 1.1 / 1.2 / 2.1) を落とし、Header / Footer 背景の Theme 適用・テスト観測点の描画値化・AppThemeBinding 回帰資産・spike だけを残す縮小提案へ改訂する。理由: 根拠の無い機構を足さない (2026-09-15)
- [付随修正] samples/ios/KsSettingsViewSample の外観適用を SwiftUI の `preferredColorScheme`（「システム」= nil）から window の `overrideUserInterfaceStyle`（「システム」= `.unspecified`）へ移した。箇所: `ContentView.swift` (適用の差し替え) / `SampleAppearance.swift` (`colorScheme` → `userInterfaceStyle`) / `SampleAppearanceWindowStyle.swift` (新規) / `KsSettingsViewSample.xcodeproj/project.pbxproj` (新規ファイルの参照・Sources 登録)。発端は tasks 0.2 で見つけた「SwiftUI クローム (大タイトル・ステータスバー・戻るボタン) が表示中のダーク切替に追随しない」症状 (オーナー指示「今対応」)。理由 (最終): iOS Sample の外観適用を MAUI (`AppTheme` → iOS では window への override) と同じ機構に揃え、iOS Native 単体を MAUI 経路の対照として使うとき window override の有無が差にならないようにする機構整備 (2026-09-15)
  - **症状への効果は主張しない (オーナー裁定 2026-09-15)**: 独立レビュー 2 周 (review-001 / review-002) の実測で、修正を外したビルドでも同じ手順で症状が再現せず、修正の有無と独立した間欠症状と判定された。証跡 `evidence/ios-sample-chrome-a-*.png` / `-b-*.png` は「症状の見え方」と「修正後ビルドで追随した状態」の記録であり、修正前後の A/B としては成立していない。写りの対応: 症状あり = `ios-sample-chrome-a-before-dark.png` / `ios-sample-chrome-b-after-dark.png`、追随 = `ios-sample-chrome-a-after-dark.png` / `ios-sample-chrome-b-before-dark.png` / `-a-after-back-to-light.png` / `-a-after-dark-2nd.png` (ファイル名の before / after は撮影時のビルドを示し、症状の有無とは対応しない)。計測条件は review-001 / review-002 に記録
  - 症状の記録 (残存): 端末ライトのまま Sample の外観選択を切り替え (例: 「ライト」→「システム」、または「ダーク」→ デモ画面へ → 戻る → 「システム」)、デモ画面を表示したまま端末をダークへ切り替えると、行 (ライブラリ) と本文は追随するのに大タイトル・ステータスバー・戻るボタンがライトのまま残ることがある。一度この状態に入ると端末外観を往復しても回復せず、再起動まで残る。失敗時の解決値を計測すると window・`UIKitNavigationController`・push 済み画面・`KsSettingsViewController` は dark で、`UINavigationBar` と stack 下段の `NavigationStackHostingController` だけが light のまま (両者の override は `.unspecified`、window に明示の `.dark` を置いても変わらない)。SwiftUI の `NavigationStack` が保持する node で Sample からは触れない
  - 裁定: 残存症状として受容して閉じる (`NavigationStack` の作り直しは外観変更のたびに root へ戻り検証目的を壊すため不採用、別 change への切り出しもしない)。回避策: Sample で外観選択を切り替えた後に表示したままの追随を確認するときは、いったんアプリを再起動する
- [蒸留時の補完] 差し戻し後、本 change は再提案されずに閉じた。残したスコープは別 change が引き取った: spike ②-b の採用 → `maui-cell-appthemebinding-logical-child` (cross/ADR-0032) / iOS の text Header / Footer 背景の Theme 適用 (Decision 4) → `scroll-indicator-visible-not-applied` (core/ADR-0032。既定は透明へ改められた) / `AppThemeBinding` の回帰資産 (tasks 4.1〜4.3) → 論理子 change の中で MauiHost の「外観追随ボタン」行・`AppearanceBindingTests.cs`・handbook `maui/integration-host-verification.md` として入った (Theme プロパティではなく Cell プロパティの binding で固定) / iOS テストの観測点の描画値化 (Decision 3、tasks 3.x) → 未着手のまま `ios-appearance-test-observe-rendered-values` として簡易起票。Decision 1 (iOS 本体の trait 観測) と Decision 2 (containment の再解決) は前提が反証されたため実装していない (2026-09-17)
