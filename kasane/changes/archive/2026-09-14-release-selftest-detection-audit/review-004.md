# レビュー結果: release-selftest-detection-audit (004 回目)

**日付**: 2026-09-14
**判定**: APPROVED

## サマリー

review-003 の 2 件は閉じた。差し替えられた検査は推奨した形と同じ効きを持ち (理由行に `::error::` を付け直す変異が名前どおりの検査で NG になる)、他の検査への波及も無い。単一誤実装 27 種を測り直して**停止 0 件・緑 0 件** (足場である安全網の除去 2 件を除く) を確認した。deviation 3 行はラベル・内容とも実態と一致している。

## 実行したテスト

| 対象 | 結果 |
|---|---|
| `scripts/release/central-portal.sh --selftest` | exit 0 / OK 77 件 / NG 0 件 |
| `scripts/release/wait-for-registries.sh --selftest` | exit 0 / OK 67 件 / NG 0 件 |
| `bash -n scripts/release/central-portal.sh` | OK |
| `python3 scripts/release/check-time-budget.py` | 緑 (publish 余裕 23 分 / wait-for-registries 余裕 8 分) |
| `comment-policy-lint.py` / `local-path-lint.py` / `identity-lint.py` | 禁止・検出 0 件 |

## 確認 1: 差し替えた検査の効き

`scripts/release/central-portal.sh:534-538` は推奨した形と同一 (理由行を 1 度だけ取り、`HTTP 503` を含むことと `::error::` を含まないことの両方を見る)。理由行に注釈の接頭辞を付け直す変異を写しへ入れて測った。

| 実装 | 結果 |
|---|---|
| 無変異 | exit 0 / OK 77 件 / NG 0 件 |
| 理由行に `::error::` を付け直す (E15) | NG 1 件 — `照会できない理由は注釈にせず標準エラーへ出す (::error::deployment の状態を照会できない (HTTP 503): abc)` |

**訂正**: review-003 で推奨修正後の件数を「OK 78 件」と書いたが、実測は **77 件**である (検査 1 件を検査 1 件へ差し替えたので総数は変わらない)。件数を数えずに書いた記述で、オーケストレーターの報告の方が正しい。

## 確認 2: 他の検査への波及と、緑 0 件

単一誤実装 27 種を測り直した。写しはスクラッチパッド、`timeout 60` で包み、リポジトリの `scripts/` は無改変。

| 変異群 | 結果 |
|---|---|
| E1〜E14 (`UNRESOLVED` の契約・連続カウント・境界・出力) | 全件 NG (NG 1〜5 件) |
| **E15 (理由行を注釈で出す)** | **NG 1 件 (前周は緑)** |
| C1〜C6 (上限判定・終端アーム・404 分岐) | 全件 NG |
| W1〜W4 (巡回上限・空回り打ち切り・照会件数のリセット / 加算) | 全件 NG |
| C10 / W5 (自己テストの安全網の除去) | 緑 (上限の「値」を短くするだけの足場。検出する検査は原理的に置けない) |

**停止 0 件・緑 0 件 (足場 2 件を除く)**。完了条件を満たしている。

`local unresolved_reason` の宣言追加による他検査への影響は無い (無変異で OK 77 件 / NG 0 件、変異時の NG も従来と同じ検査が名指しで落ちる)。

## 所見 (対応不要)

- **E2 で自己テストが途中で止まる**: `deployment_state` を `fail` へ戻す変異 (E2) を入れると、新しい `unresolved_reason="$(...)"` が素の代入であるため errexit で自己テストがその場で終わる (NG 3 件を出した時点で残りが走らない)。検出は成立しており (exit 1)、中断が NG を緑へ変えることは無いので実害は無い。変異を入れたときだけ起きる挙動で、無変異では `deployment_state` が常に 0 を返すため中断しない

## deviation の照合

| 行 | ラベル | 内容と実装の一致 |
|---|---|---|
| 1 | `[決定事項と違う形]` | 一致 (空回り巡回の 2 段目の歯止め) |
| 2 | `[決定事項の緩和]` | 一致 (連続回数での失敗。`cmd_wait_validated` は即時失敗のまま) |
| 3 | `[決定事項と違う形]` | 一致。ラベルが実態 (決定事項 1 の実現方法の変更) に合った |

## 指摘事項

なし。

### 確認した観点

仕様充足 (exploration の決定事項 1〜4 と塞ぐ範囲)、足場アーティファクト (`exploration.md`) の不改変、deviation 記録と実装の一致、本番の呼び出し文脈での挙動 (review-003 で実測済み・今周は変更なし)、自己テストの検出力 (誤実装スイープ 27 種)、`handbook/cross/comment-policy.md` と既存コードの語彙・密度との整合、リリースの時間予算 (`check-time-budget.py`)。
