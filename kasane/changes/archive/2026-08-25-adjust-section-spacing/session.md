# Live Session: adjust-section-spacing
対象: Section Header / Footer の上下余白調整 (iOS/Android で差異あり。Android は上下が詰まりすぎ)。まず Sample の Section 装飾デモを iOS で調整
開始: 2026-08-25

## 試行ログ (append-only)
- 準備: 対象特定。iOS Modern の Section margin = ios/Sources/KsSettingsViewUI/SectionBoxMetrics.swift:31 modernDefaultMargin (top 22 / bottom 0)、Header/Footer ラベル内側余白 = KsSettingsViewController.swift:2348-2363 (上下 2pt)。Android 対応値 = SectionBoxMetrics.kt:56-57 MODERN_DEFAULT_MARGIN (top 12 / bottom 12)、SectionAccessoryViewHolders.kt:299 TextView 上下 padding 0。差異の中心は Modern margin の top (iOS 22 vs Android 12) → 継続

- 試行1 (iOS): Header ラベル下余白 / Footer ラベル上余白を 2pt → 4pt (KsSettingsViewController.swift:2348 の `-2` → `-4`、:2359 の `2` → `4`)。反対側の `>=` / `<=` 制約は 2pt のまま。ビルド成功、シミュレータ (iPhone 17 / iOS 26.0) の Section 装飾デモに反映済み → ユーザー確認待ち
- 確認: Section 装飾デモの既定状態は style = Modern・装飾プリセット = 既定 (SectionDecorationDemoView.swift:28-29 / SectionDecorationPreset.swift standard は sectionMargin nil)。よって Modern 既定 margin (top 22 / leading 16 / bottom 0 / trailing 16) が効いている。Classic (全方向 0) になるのは画面上の Classic/Modern セグメントを Classic に切り替えたときのみ
- 試行2 (iOS): Classic 既定 Section Margin を上下のみ Modern と同値に (SectionBoxMetrics.swift:33 `classicDefaultMargin` top 0 → 22 / bottom 0 据置)。水平は `resolve` が `.classic` で leading/trailing を常に 0 に落とす実装 (SectionBoxMetrics.swift:58-62、Section 境界を全幅に保つため) なので値を入れても無効化される → 水平は 0 のまま。ビルド成功、Classic 表示で Section 上に 22pt の余白が出ることを確認 → ユーザー確認待ち
- 試行3 (iOS): オーナー指示により `classicDefaultMargin` を Modern と完全同値へ (SectionBoxMetrics.swift:37 leading/trailing 0 → 16。top 22 / bottom 0 は試行2 のまま)。`resolve` の分岐は変更なし。ビルド成功、Classic 表示は試行2 と同一 — Cell 行は全幅のままで左右インセットは出ず、水平成分が実際に無視されることを実物で確認
- 試行4 (Android): iOS 確定値へ同値化。SectionBoxMetrics.kt:57 `MODERN_DEFAULT_MARGIN` top 12dp→22dp / bottom 12dp→0dp (水平 16dp 据置)、:60 `CLASSIC_DEFAULT_MARGIN` `PaddingValues(0.dp)`→`MODERN_DEFAULT_MARGIN` 同値、SectionAccessoryViewHolders.kt bind に `applySectionTextVerticalPadding` を追加 (Header 下 4dp / Footer 上 4dp、水平 16dp は据置)。Classic の上下 margin は `resolve` が isModern 分岐せず反映・水平のみ Classic で 0 に落とす実装 (SectionBoxMetrics.kt:105-106) で iOS と同仕様であることを確認。ビルド成功、エミュレータ (ksn_custcell_api35 / API 35) の Section 装飾デモに反映済み → ユーザー確認待ち
- 調査 (Android, 編集なし): SwitchCell のオフ色はライブラリ側が bind ごとに上書きしていた。SwitchCellViewHolder.kt:67-74 で `thumbTintList` / `trackTintList` を組み立てるため `materialSwitchStyle` の既定 tint は効かない。オフ色は固定 RGB ではなく Material3 attr 由来 (thumb = `colorOutline` / track = `colorSurfaceContainerHighest`、解決失敗時のみ `Color.GRAY` / `Color.LTGRAY` のフォールバック)。オン色は thumb = `colorOnPrimary` / track = `cell.accentColor ?: effective.accentColor`
- 対象拡大: Section 余白に加えて SwitchCell のオフ状態色も本 change のライブ調整対象に含める (オーナー指示)
- 試行5 (Android): SwitchCell オフ色を accent 由来へ。SwitchCellViewHolder.kt の `colorOutline` / `colorSurfaceContainerHighest` 直参照を廃し、`offTrackColorFrom` / `offThumbColorFrom` の2ヘルパを追加。導出は下地 (`colorSurfaceContainerHighest`) に accent を載せる blend で、track = accent 28%、thumb = accent 55% + 彩度 45% へ減彩 (係数は `OFF_TRACK_ACCENT_RATIO` / `OFF_THUMB_ACCENT_RATIO` / `OFF_THUMB_SATURATION_RATIO` に定数化)。オン状態は変更なし。ビルド成功、エミュレータの Section 装飾デモ (機内モード行) で accent 緑由来の淡緑 track + 減彩緑 thumb を確認 → ユーザー確認待ち
- 試行6 (Android): 試行5 のオフ色にオーナー評価「thumb がもっと暗くないと見えない / track はもっと彩度低く薄く」を反映。SwitchCellViewHolder.kt の係数を track: `OFF_TRACK_ACCENT_RATIO` 0.28→0.14 + `OFF_TRACK_SATURATION_RATIO` 0.45 を新設、thumb: `OFF_THUMB_LIGHTNESS_RATIO` 0.55 を新設 (blend 0.55 / 彩度 0.45 は据置)。HSL の彩度・明度スケールは `Int.scaleHsl(saturationRatio, lightnessRatio)` 拡張関数に集約。ビルド成功、エミュレータで thumb が暗い減彩緑・track がほぼ無彩の淡色になったことを確認 → ユーザー確認待ち
- 確定 (Android): 試行6 の track 導出をオーナーが採用 (`OFF_TRACK_ACCENT_RATIO` 0.14 / `OFF_TRACK_SATURATION_RATIO` 0.45)
- 試行7 (Android): オーナー評価「thumb はさらに彩度を下げて緑みを帯びた灰色くらいに (暗さは現状維持)」を反映。SwitchCellViewHolder.kt `OFF_THUMB_SATURATION_RATIO` 0.45→0.16 のみ変更 (blend 0.55 / 明度 0.55 は据置、track も据置)。ビルド成功、エミュレータで thumb が accent の色相をかすかに残す暗い灰色になったことを確認 → ユーザー確認待ち
- 調査 (Android, 編集なし): Switch オフ時の track 枠線 (`trackDecoration`) の色はライブラリ未指定で Material3 テーマ既定由来。`material-1.12.0.aar` の `Widget.Material3.CompoundButton.MaterialSwitch` が `trackDecorationTint = @color/mtrl_switch_track_decoration_tint` を指定し、その selector は checked = transparent / unchecked = `m3_comp_switch_unselected_track_outline_color` (= `colorOutline`) / disabled = 同色を低不透明度、という構成
- 試行8 (Android): thumb 明度 `OFF_THUMB_LIGHTNESS_RATIO` 0.55→0.65 (オーナー評価「もう少し明るく」)。あわせて SwitchCellViewHolder.kt の bind で `trackDecorationTintList` を明示設定し、オフ時の枠線色を thumb と同一の導出色に、オン時は Material3 既定と同じ透明に揃えた (オン時の見た目は不変)。ビルド成功、エミュレータで枠線が thumb と同色の灰緑になったことを確認 → ユーザー確認待ち
- 確認 (MAUI): MAUI 側に Section Margin の追随編集は不要。ライブラリ層 (SettingsView.cs:443 `SectionMarginProperty` は `default(Thickness?)`、KsThemeSnapshot / KsBridgeGateway は値をそのまま輸送) は既定値を複製せず null をネイティブへ委譲。サンプルの明示値は SectionDecorationPreset.cs:36 の「余白広め・角丸小」プリセット (32/32/32/0) のみで、旧既定 12 を前提にした値はなく iOS/Android サンプルのプリセットと同値 → 変更なし
- 試行9 (Android / **一時変更 (要復元)**): オフ色の色相追従を見比べるため、Sample の SectionDecorationDemoScreen.kt の SwitchCell 3 件に検証用 accentColor を一時付与。機内モード = 赤 `0xFFD32F2F` / 自動 = 青 `0xFF1976D2` / True Tone = オレンジ `0xFFF57C00`。定数 `TEMP_ACCENT_RED` / `TEMP_ACCENT_BLUE` / `TEMP_ACCENT_ORANGE` と各 `accentColor =` 指定には `TODO: 一時変更 (要復元)` を付記済み。**確定時にこの4箇所 (定数定義 + 3 Cell の指定) と `androidx.compose.ui.graphics.Color` の import を削除して元に戻すこと**。ライブラリ側 (SwitchCellViewHolder.kt) は無変更。ビルド成功、エミュレータで各行のオフ/オン色が accent の色相へ追従することを確認
- 試行10 (Android): スクショ実測比較 (M3 既定オフ thumb = `colorOutline` は彩度 3%、試行8 時点の導出 thumb は彩度 9〜13%) を受けたオーナー指示で、SwitchCellViewHolder.kt `OFF_THUMB_SATURATION_RATIO` 0.16→0.08 (blend 0.55 / 明度 0.65 は据置、track・`trackDecorationTintList` の仕組みも据置 — ボーダーは thumb 同色のまま連動して薄まる)。ビルド成功、エミュレータで検証用 accent 3 色 (赤/青/オレンジ) の Switch を全てオフにして並べ、いずれも M3 既定に近いほぼ無彩の thumb になったことを確認 → ユーザー確認待ち
- 確定時テスト (iOS): `cd ios && swift test` は macOS 実行で Core の 93 件のみ (UIKit 依存の UI テストは走らない)。UI テストは `xcodebuild test -scheme KsSettingsView-Package -destination 'platform=iOS Simulator,...'` で実行 → 588 件中 10 件失敗。同一コマンドを変更前 (git checkout で 2 ファイルを HEAD へ戻した状態) の該当4スイートで実行すると 67 件全 pass のため、10 件はすべて今回の変更起因の期待値ズレ (既存不具合ではない)。テスト側の期待値更新は未実施 (指示により編集なし)
- 試行11 (Android): オーナー指示でオフ track の明度を +5% (実測 L 84〜85% → 90% 前後、M3 既定と同水準)。SwitchCellViewHolder.kt に `OFF_TRACK_LIGHTNESS_RATIO` = 1.07 を新設し `offTrackColorFrom` の `scaleHsl` 第2引数へ適用 (彩度 0.45 / blend 0.14 は据置)。ビルド成功、エミュレータ + 実機 Pixel 6a の両方に反映
- 確定 + 一時変更復元 (Android): オーナーが確定を宣言。試行9 の Sample 検証用 accent (TEMP_ACCENT_RED/BLUE/ORANGE の定数・3 Cell の指定・`Color` import) をすべて削除し、`git status` で `samples/` の差分ゼロを確認。最終ビルド成功、エミュレータ + 実機 Pixel 6a に最終 APK を反映済み
- **エスカレーション (未解決)**: `./gradlew test` で 5 テストクラスが失敗。うち 3 件は今回の確定値を旧値で固定している既存テスト (SectionBoxMetricsTest「上余白の既定は 12dp」「Classic の未指定は上下 0 余白」、SectionAccessoryRenderingTest「TextView 上下 padding は AiForms 準拠で 0」、ClassicSectionDecorationTest「sectionMargin 未指定なら Section の前後に余白を入れない」)。残る 2 件 (CustomCellRecycleTest「埋め込みの factory が再実行されている」、KsBridgeOperationContractTest「position 0 の行が実描画されていない」) は変更前の worktree では通るため今回の margin 変更が引き金と判明。前者は期待値が仕様文言 (AiForms 準拠 / 従来の Classic と一致) を根拠にしているためワーカー判断で書き換えず、後者は原因調査が必要 → ユーザー判断待ち
- テスト期待値更新 (確定値は不変): SectionBoxDecorationTests.swift 3件 (L143 実効値検証へ書き換え+改名 / L1184 改名+22pt期待へ / L522 Header ラベル 4pt 分を加算)、AccessoryViewLiveProbeTests.swift・AccessoryMeasureInvalidationTests.swift・KsBridgeAccessoryViewTests.swift の Root header 高さを「内容高さ + 22」へ。再実行で xcodebuild 588件 / swift test 93件とも全 pass
- 調査 (a) concepts/decisions の明文化確認: (1) 「Classic の Section 既定 margin は上下 0」は **明文化あり** — `kasane/concepts/core/styling/list-appearance.md:56-58` の解決表「Classic: 上下 0 / Modern: platform 既定寸法」と `kasane/concepts/core/styling/style-resolution.md:48`「style 別の platform default (Classic: margin 上下 0 / Modern: platform 所有の既定寸法)」。なお水平は「既定 0」ではなく「Classic では leading/trailing を無視する (全幅契約)」という別ロジックとして記述 (list-appearance.md:71)。(2) 「Header/Footer TextView の上下 padding 0 / AiForms 準拠」は **明文化なし** (concepts/decisions のどこにも該当記述なし。roadmaps の AiForms 棚卸しは「対応 API が無い」の意、archive の類似記述は iOS の headerTopPadding や Picker シートの話で別物)。(3) 「Modern の既定 margin 上下 12dp」は list-appearance.md:65 に**日付付きの参考値**としてのみ存在し、契約値ではない (同 100 行で「既定 margin は platform 間で統一しない」と明記)
- 試行12 (Android, テスト更新): (a) の判定に従い、明文化のない 3 ケースだけ確定値の言葉へ更新。SectionBoxMetricsTest「上余白の既定は 12dp/下 12dp」→ 22dp / 0、SectionAccessoryRenderingTest の Header/Footer 2 ケースをテスト名ごと「Cell 側 (下/上) にだけ 4dp の余白を持つ」へ改名し期待値 0→4dp。あわせて SectionAccessoryViewHolders.kt の `createSectionTextView` KDoc から根拠のない「AiForms オリジナル準拠で上下 0」の記述を外し、bind 側で Cell 側にだけ余白を入れる旨に書き換え。該当 3 ケースは PASS
- **停止報告 (a-未実施)**: Classic 関連の 2 ケース (SectionBoxMetricsTest「Classic の未指定は上下 0 余白で従来の表示と一致する」/ ClassicSectionDecorationTest「sectionMargin 未指定なら Section の前後に余白を入れない」) は上記のとおり concepts に明文化された規定に真正面から抵触するため**編集せず保留**。確定値 (Classic = Modern 同値) を通すなら concepts/core/styling/list-appearance.md:56-58 と style-resolution.md:48 の更新が先 → ユーザー判断待ち
- 調査 (b) 残り 2 ケースの切り分け → **テスト環境の幾何の問題** (実挙動の回帰ではない)。根拠: `KsSettingsView.kt:154` のとおり既定 style は Classic で、両テストとも style を明示しないため Classic 既定 margin が効く。今回 Classic の上余白が 0→22dp になり Section ごとに縦の内容量が増えた一方、両テストは RecyclerView を `metrics.heightPixels` (画面 1 枚分) で `measure`/`layout` した上で「全 adapter position が同時に実描画されている」(KsBridgeTestHost.kt:122-129 `renderedRows`) / 「端から端へスクロールしても CustomCell の ViewHolder は 1 つだけ」(CustomCellRecycleTest.kt:198-206) を前提にしている。診断として `CLASSIC_DEFAULT_MARGIN` だけ一時的に 0 へ戻すと両テストとも PASS し、他の値 (Modern margin / H/F padding / Switch 色) は無関係と確認 (診断後すぐ確定値へ復元済み)。Modern は 12+12=24dp → 22+0=22dp と縦量が減るため無影響。修正案: (案1) 両テストのレイアウト高さを画面 1 枚分から十分な高さ (例: `heightPixels * 3`) へ広げる — 最小変更で assertion の意図を保てる。(案2) 両テストが Theme に `sectionMargin = PaddingValues(0.dp)` を明示指定し、ライブラリ既定値の変動から独立させる — 幾何を前提にするテストとしてはこちらが頑健。いずれもテスト側のみの修正でプロダクトコードは触らない
- テスト現況: `./gradlew test` は 4 ケース失敗 (ui 3 / bridge 1)。内訳は上記の Classic 保留 2 + 幾何問題 2。他 1100 件超は PASS
- 試行13 (Android, テスト更新 (a)): オーナー判断「本セッションで実物確認のうえ確定した仕様変更なのでテストを新仕様の言葉へ」に従い Classic 保留 2 ケースを更新。SectionBoxMetricsTest → `Classic の未指定は上下余白が Modern と同値になり水平は無視される` (上 22dp / 下 0 / 左右 0 を個別メッセージ付きで検証)、ClassicSectionDecorationTest → `sectionMargin 未指定なら Classic 既定の上余白が Section の先頭に入る` (先頭行に `CLASSIC_DEFAULT_MARGIN` 由来の上 offset、下・左右は 0)。旧仕様文言「従来の Classic と同じく offset は 0」「従来の表示と一致する」は撤去
- **concepts 追随は distill 送り**: `kasane/concepts/core/styling/list-appearance.md:56-58` と `style-resolution.md:48` の「Classic: 上下 0」記述は本 change では更新せず、蒸留フェーズの責務として持ち越す (同 list-appearance.md:65 の Modern 12dp 参考値も同様)
- 試行14 (Android, テスト更新 (b)): 案2 を採用し、幾何前提のテスト 2 件をライブラリ既定値から独立させた。CustomCellRecycleTest は `PINNED_GEOMETRY_THEME = Theme(sectionMargin = PaddingValues(0.dp))` を companion に定義し `showCells` の `setRootDirect(..., theme = ...)` で渡す (`settingsView.theme` への代入は `setRootDirect` の既定引数 `Theme()` に上書きされるため無効だった — KsSettingsView.kt:495)。KsBridgeOperationContractTest は `pinnedGeometryTheme()` を追加して attach 直前の `setTheme` と case 12 (setTheme) の双方へ適用。いずれも「全行同時描画 / 単一 ViewHolder の幾何を前提にするため margin を固定する」旨のコメント付き。プロダクトコードは無変更
- テスト最終結果: `./gradlew test` **BUILD SUCCESSFUL** (全 pass)。`samples/` は差分ゼロを維持
- review-001 指摘対応 (iOS、確定値・見た目は不変): (1) `classicDefaultMargin` をリテラル重複から `= modernDefaultMargin` の別名方式へ (Android の CLASSIC_DEFAULT_MARGIN と同方式)、(2) Header 下 / Footer 上 4pt を `KsSettingsViewController.sectionTextGap` へ定数化 (Android の SECTION_TEXT_GAP_DP と同値)、(3) SectionBoxLayout.swift:24 の初期 metrics を `SectionBoxMetrics.resolve(theme: Theme(), style: .classic)` 経由へ **修正実施** — `metrics` の読み出しは同ファイル内の cornerRadius / borderWidth / borderColor のみで `margin` は全コード中どこからも読まれない (grep で外部参照ゼロを確認) ため、resolve 経由でも消費される3値は完全同一で挙動不変。xcodebuild 588件 / swift test 93件とも全 pass
- オーナー決定: **Root Header/Footer の余白は 0 で統一**（Android は既に 0dp）。Root は利用者がカスタム View を差し込む場所なので、ライブラリ側で余白を入れない。旧実装では 2pt 時代から Root にも波及していたため、これは「Root を 2→4pt にする」ではなく「Root は 0 にする」意図的変更。
- 対応 (iOS): `KsSettingsViewController` に `rootTextGap = 0` と `textGap(forElementKind:)` を追加し、Root/Section の分岐点を1箇所に集約。`applyAccessoryToListCell` / `applyAccessoryLabel` に `textGap` を通し、テキストの背に当たる側の最小余白も `textGap > 0 ? 2 : 0` として Root では 0 に。Section は従来どおり gap 4 / 最小 2 で見た目不変。
- 計測メモ: Root Header 領域は `.estimated(20)` が下限として残るため、テキスト（高さ 15.67）は下端揃えで領域下端に密着し、上に 4.33pt の領域スラックが残る（ライブラリの余白ではない）。この影響で `test_RootHeaderがあると上余白はRootHeaderと先頭Sectionの間に入る` の第3アサートは等値比較が成立しなくなるため「さらに下へ押し下げられない」ことを見る不等号に変更。`test_可視Sectionが0件ならRootHeader内側の余白も0になる` は +4 を戻して 0 / 24 基準へ。
- 検証: xcodebuild 588件 / swift test 93件とも全 pass。シミュレータの DSL 方式デモ (Root H/F あり) で Root Header 直下に余分な余白が無く、Section Header の 4pt は保たれていることを目視確認。
- 試行15 (Android, review-001 指摘対応 1〜4): [Must1] `SwitchCellAccentDerivationTest.kt` を新設 (7 ケース: オフ色が attr 直値でない / accent を変えるとオフ色も変わる / オフ Track の色相が accent に追従 / Cell 個別 accentColor も導出に効く / オフ borderは thumb 同色・オン時透明 / オン Track は accent そのもの・オン Thumb は colorOnPrimary / ダークで Thumb > Track の明度差)。accent 比率を 0 にする変異で 3 ケースが落ちること、明度を固定係数に戻す変異でダークのケースが落ちることを確認済み。[Must2] BasicCellsTest.kt の旧仕様記述 5 箇所 (オフ Track = colorSurfaceContainerHighest / オフ Thumb = colorOutline) を現行仕様の言葉へ更新。[Minor3] SectionAccessoryViewHolders.kt の行コメント「上下は AiForms オリジナル準拠で 0」を KDoc と整合する記述へ。[Suggestion4] `HSLToColor` が alpha を落とす件を `ColorUtils.setAlphaComponent` で元色の alpha 保持に修正 (不透明 accent では出力不変)
- 調査 (5) ダークモード実測 (修正前): エミュレータを night yes にして計測したところ **明度差がほぼ潰れていた** — off thumb L 24% / off track L 26% (差 2pt)。レビューの試算どおり、ライト実測ベースの固定係数乗算がダークの暗い土台をさらに乗算するのが原因
- 試行16 (Android, ダーク対応): オーナー方針「素の MaterialSwitch の模倣」に従い導出を再構成。`tintedFrom(base, accent, saturation, lightnessRatio)` を新設し、**明度はテーマ attr から取り (track = `colorSurfaceContainerHighest` / thumb = `colorOutline`)、色相を accent、彩度を確定した淡さの絶対値** で作る形へ。係数は `OFF_TRACK_SATURATION` 0.09 / `OFF_THUMB_SATURATION` 0.04 / `OFF_TRACK_LIGHTNESS_RATIO` 1.0 / `OFF_THUMB_LIGHTNESS_RATIO` 0.92 (旧 blend 比率 + 明度乗算の 6 定数は廃止)。実測比較 — ライト: thumb (H135 S3.6% L43.5%) → (H133 S4.0% L43.7%)、track (H144 S9.1% L89.2%) → (H132 S9.4% L89.6%) で **確定した見た目を維持** (ΔRGB ≤ 2/ch)。ダーク: thumb L 53.3% / track L 21.8% と明度関係が反転・差 31pt へ回復し、素の M3 と同じ「thumb が track より明るい」関係になった。証跡: `kasane/changes/adjust-section-spacing/evidence/switch-off-colors-light.png` / `switch-off-colors-dark.png`
- テスト: `./gradlew test` **BUILD SUCCESSFUL** (全 pass)。`samples/` は差分ゼロを維持
- 調査 (Android): オーナー指摘「ダークでオンの thumb が青っぽい」を確認 → 事実。オン thumb だけ `colorOnPrimary` を参照していた (SwitchCellViewHolder.kt:117 旧) ため、ライトでは白に解決される一方ダークではテーマ primary の暗トーン (紫紺) に解決され、track が accent 由来なのに thumb だけテーマ色が漏れていた
- 試行17 (Android): オン thumb をテーマ attr 依存から外し、`onThumbColorFrom(accent)` で accent に対するコントラスト色として決める形へ (素の M3 の track=primary / thumb=onPrimary の関係を accent 基準で模倣)。白のコントラスト比が `ON_THUMB_MIN_CONTRAST` = 1.5 以上なら白、下回る明色 accent のみ accent の色相を残した暗色 (`ON_THUMB_DARK_SATURATION` 0.10 / `ON_THUMB_DARK_LIGHTNESS` 0.15) へ倒す。**閾値の根拠**: 当初 WCAG 非文字基準の 3.0 で実装したところ、緑 #34C759 (白比 2.22)・橙 #FF8D28 (2.31)・青緑 #00C3D0 (2.16) といった一般的な accent が軒並み暗色へ倒れてライトの見た目が変わってしまうため 1.5 へ下げた (つまみは文字ではなく面積のある図形で、素の M3 も同水準の比で白 thumb を置く)。淡黄 #FFF099 (1.16) のような明色のみ暗色になる。オン track (accent そのもの) は無変更
- 実測 (試行17 後): ライト = off thumb RGB(107,116,109) / off track (226,231,227) / **ON thumb (255,255,255)** / ON track (52,199,89)。ダーク = off thumb (131,141,134) / off track (51,60,53) / **ON thumb (255,255,255)** / ON track (52,199,89)。ライトは試行16 から一切変化なし、ダークのオン thumb が紫紺 → 白に是正され両モードで一致
- 試行18 (Android, review-002 Minor 3 件): SectionAccessoryRenderingTest.kt の区切りコメント「AiForms と揃え、上下 padding は 0」→「Cell 群に面する側にだけ 4dp」へ / BasicCellsTest.kt のオフ色分離の説明を「係数差」→「明度の土台 attr の違い (colorSurfaceContainerHighest vs colorOutline) が主因、ダーク追従もそこで担保」へ / SwitchCellAccentDerivationTest.kt のクラス KDoc にオン thumb 2 件とダーク明度差の検証範囲を追記
- 回帰テスト追加 (試行17): `オン状態の Thumb はテーマが変わっても同じ色になる` (Light/Dark Context で同色 + ダークの colorOnPrimary と不一致) と `明るい accent ではオン Thumb が暗色へ倒れて視認性を確保する` の 2 件。`colorOnPrimary` 参照へ戻す変異で両方が落ちることを確認済み
- テスト最終結果: モジュール順次実行で全 pass (ui 1934 / bridge 322 / compose 240 / core 160 = 計 2656、failures 0 / errors 0。debug+release 変種を含む集計、ui の debug 単体は 967)。※ 途中 `./gradlew test` が `java.io.EOFException` で落ちる事象は、エミュレータ稼働と他 Gradle デーモンとの同時実行によるテストワーカーの資源枯渇 (アサーション失敗ではない) — 順次実行で解消
- 試行19 (Android, review-003 Minor 2 件・文言のみ): BasicCellsTest.kt:1106-1110 の KDoc から撤去済みの `colorOnPrimary` 記述を外し「オン時は accent に対するコントラスト色 (通常は白)、オフ時は accent の色相を `colorOutline` の明度に載せた減彩色。いずれも `colorOnPrimary` は参照しない」へ / 同 :1155 の assert メッセージ「accent 由来の淡色 / accent 由来の減彩・暗色」をモード非依存の「`colorSurfaceContainerHighest` の明度 / `colorOutline` の明度」へ。`:ks-settingsview-ui:compileDebugUnitTestKotlin` BUILD SUCCESSFUL (挙動不変のため再テストなし)。`colorOnPrimary` の残存参照は「参照しない」旨の説明・退行検出テストの対象・素の M3 の説明のみで、現在仕様として語る箇所はゼロを grep で確認

## 決定事項
- iOS 確定 (オーナー実物確認 + Classic/Modern 切替の違和感解消を確認済み):
  - Header ラベル下余白 / Footer ラベル上余白 = 4pt (KsSettingsViewController.swift:2348 / :2359)
  - classicDefaultMargin = modernDefaultMargin と完全同値 (top 22 / leading 16 / bottom 0 / trailing 16)。水平は resolve が .classic で 0 に落とす仕様どおり無視されることを実物確認 (SectionBoxMetrics.swift:58)
- 次: Android を同値に揃える (MODERN_DEFAULT_MARGIN top 12→22dp / bottom 12→0、CLASSIC も同値化、Header/Footer TextView の上下 padding に 4dp 相当)
- Android 確定 (オーナー実物確認済み): 試行4 の同値化をそのまま採用 (MODERN top 22dp / bottom 0dp、CLASSIC = MODERN 同値、Header 下 4dp / Footer 上 4dp)
- Switch オフ色確定 (オーナー実物確認済み、2026-08-25): accent 由来の導出を採用。track = 下地 (colorSurfaceContainerHighest) に accent 14% blend + 彩度 45%、thumb = accent 55% blend + 彩度 8% + 明度 65%、オフ時ボーダー (trackDecorationTintList unchecked) = thumb と同色。thumb 彩度 8% はスクショ実測で M3 既定 (colorOutline 彩度 3%) に寄せた値。オン状態は不変
- MAUI: 編集不要を確認 (SectionMargin 既定 null をネイティブへ委譲、既定値の複製なし)。ネイティブ確定値が MAUI 経由でも反映されることをシミュレータで確認済み
- 最終確定 (2026-08-25): track に明度 +5% (OFF_TRACK_LIGHTNESS_RATIO 1.07、実測 L 90% 前後で M3 既定と同水準) を追加した状態を、検証用3色復元後の最終ビルドで実機 Pixel 6a にデプロイし、オーナーが実機確認して確定を宣言。以降はテスト整備 (iOS 期待値更新 / Android (a)(b) 判定) と summary 清書のみ — 見た目・挙動の変更は行わない

## エスカレーション・スコープ外の発見
- concepts 抵触 (distill 送り): 「Classic の既定 sectionMargin は上下 0」が kasane/concepts/core/styling/list-appearance.md:56-58 と style-resolution.md:48 に明文化されており、本 change の確定仕様 (Classic 既定 = Modern 同値の上下 margin) と抵触する。決定はオーナーが実物確認のうえ本セッションで確定済みのため覆さない。concepts の追随更新 (該当2ファイル) は ksn-distill で行うこと。あわせて「Classic/Modern の既定 sectionMargin を同値に統一 (水平は Classic で無視)」は ADR 候補
- Android の Modern 既定 12dp は list-appearance.md:65 に「(2026-08-20 時点)」の参考値として記載 → 22dp への追随も distill で更新
- 教訓候補 (蒸留時に ksn-lesson へ): 「実装を変えたのに、その仕様を現在形で語る KDoc / テスト名 / assert メッセージが古いまま残る」というコメント陳腐化の指摘が review-001 / 002 / 003 と **3 周連続** で出た (対象: AiForms 準拠の padding 0、オフ色のテーマ attr 直参照、オン thumb の colorOnPrimary、ライト限定の色表現)。値の変更に追随して周辺の説明文を洗う手順が実装フローに無いことが原因で、レビュー往復のコストになっている
