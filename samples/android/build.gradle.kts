// KsSettingsView Android Sample アプリ — ルートビルドファイル
//
// 本ファイルではプラグインを宣言しない（各サブプロジェクトの build.gradle.kts で宣言する）。
// `includeBuild("../../android")` 経由で本体モジュール（ks-settingsview-core / ui / compose）が
// 利用可能になる。
